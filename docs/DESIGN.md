# 実装方針・調査結果（設計ドキュメント）

このドキュメントは、「楽天ROOM運用の自動化」をどこまで・どうやって進めるかをまとめたものです。
専門用語はできるだけ避けていますが、わからない言葉が出てきたら遠慮なく質問してください。

## 1. 結論（まずここだけ読めばOK）

- 商品を探す・条件で絞り込む・重複をチェックする・紹介文を作る・候補一覧を保存する
  → **公式のデータだけを使い、自分のパソコン（このプロジェクト）の中だけで完結する処理なので、自動化して問題ありません。**
- 楽天ROOMへ実際に投稿するボタンを押す部分
  → **自動化しません。** 楽天ROOMの利用規約で「機械的に投稿する行為」がはっきり禁止されているためです。
  最終的に「コピペして投稿する」のは人間（あなた）が行います。

つまりこのプロジェクトは「投稿の下ごしらえを全自動化し、投稿ボタンを押す瞬間だけ人間が行う」という設計にします。

## 2. 調べたこと

### 2-1. 楽天ROOMの利用規約について

楽天ROOMの利用規約（禁止事項）には、次のような趣旨の記載があります。

> スクリプトその他のプログラムを実行する等の方法により、機械的に投稿、コメント等をする行為を禁止する

参考：
- [利用規約 - ROOM - 楽天市場](https://room.rakuten.co.jp/signup/agreement)
- [投稿のガイドライン - ROOM - 楽天市場](https://room.rakuten.co.jp/info/help/guideline/01.html)

つまり「ROOMへの投稿を、プログラムが自動でボタンを押して行う」ことは規約違反にあたる可能性が高く、
アカウント停止（BAN）のリスクがあります。世の中には「自動投稿ツール」を作って公開している人もいますが、
規約違反のリスクを利用者が負う形になっているため、このプロジェクトでは採用しません。

### 2-2. 楽天が公式に提供しているAPI（データ取得の仕組み）

楽天には「楽天ウェブサービス（Rakuten Web Service, 通称RWS）」という、開発者向けに公式に提供されている
データ取得の仕組み（API）があります。この中の「楽天市場 商品検索API（Ichiba Item Search API）」を使うと、
以下のような情報を**合法的に**取得できます。

- 商品名、価格、商品ページのURL、商品画像のURL
- レビュー平均点（reviewAverage）
- レビュー件数（reviewCount）
- ショップ名、ジャンル（カテゴリ）情報
- 商品の説明文（キャッチコピー）

参考：
- [Rakuten Web Service: Rakuten Ichiba Item Search API](https://webservice.rakuten.co.jp/documentation/ichiba-item-search)

利用するには「楽天会員登録」と「楽天ウェブサービスの利用登録（無料）」を行い、
「アプリID（applicationId）」と呼ばれる鍵となる文字列を発行してもらう必要があります。
2026年のAPI基盤刷新以降は、これに加えて「アクセスキー（accessKey）」も必要になりました。
これらは合言葉のようなもので、他人に見せたり、GitHubなどにそのまま公開したりしてはいけません
（このプロジェクトではGitHubのRepository Secretsに保存し、コードやログには一切出力しません）。

このAPIには「1秒間に送れるリクエストの数」などの利用制限（レート制限）があります。
短時間に大量アクセスすると制限にかかったり、利用停止になる可能性があるため、
プログラム側で「少し間隔を空けて呼び出す」処理を必ず入れます。

> 補足：APIのレスポンスに「楽天ROOM対象商品かどうか」を直接示す項目は現時点では見当たりませんでした。
> 実務上は「楽天市場で購入できる商品はROOMにも投稿できる」という前提で問題ありませんが、
> 実際に運用しながら例外がないか確認していきます。

### 2-3. ログイン・認証の扱いについて

このプロジェクトで「ログイン」が必要になるのは、商品データを取るための
「楽天ウェブサービスのアプリID」だけです。これはIDとパスワードでログインするタイプの認証ではなく、
発行された文字列（鍵）をプログラムに渡すだけの仕組みです。

- 楽天ROOM自体へのログイン（メールアドレスやパスワード）は、このプロジェクトでは**一切扱いません**。
  自動投稿をしない設計にしているため、ROOMにログインする必要自体がないからです。
- アプリID（鍵）は `.env` という設定ファイルに書き、`.gitignore` でGitの管理対象から除外します。
  これにより、間違ってGitHubに公開してしまう事故を防ぎます。

### 2-4. BANリスクを避けるための方針まとめ

| やること | 安全性 |
|---|---|
| 公式API（楽天ウェブサービス）でデータ取得 | ◎ 安全（利用規約で認められた使い方） |
| 取得したデータをローカルで条件判定・重複チェック | ◎ 安全（自分のPC内の処理） |
| 紹介文をテンプレートで自動生成 | ◎ 安全（自分のPC内の処理） |
| 候補一覧をファイルに保存し、人間が見て選ぶ | ◎ 安全 |
| ROOMのサイトを自動操作してログイン・投稿する | × 行わない（規約違反・BANリスク） |
| ROOMのページをスクレイピング（プログラムで読み取り）する | × 行わない（規約違反・負荷をかけるリスク） |

## 3. 自動化する処理の全体像

```
[1] 商品候補の収集
     楽天ウェブサービスAPIに「掃除」「収納」「キッチン 便利グッズ」などのキーワードで検索をかけ、
     商品情報（価格・レビュー評価・レビュー件数など）を取得する。

        ↓

[2] 条件判定（フィルタリング）
     以下の条件を満たす商品だけを残す。
       ・レビュー評価が4.0以上
       ・レビュー件数が100件以上
     （広告対象かどうか、ポイント倍率、ランキング入賞、メディア掲載の有無は判定材料にしない）

        ↓

[3] 重複チェック
     「過去に紹介した商品の一覧（data/posted_items.json）」と商品IDを突き合わせ、
     すでに紹介した商品を候補から除外する。

        ↓

[4] 紹介文の自動生成
     商品名・特徴・価格などから、
       ・自然な日本語の紹介文
       ・箇条書き（特徴のポイント）
       ・ハッシュタグ
     を1つの文章ブロックにまとめる（500文字以内、コピペしてそのまま使える形）。

        ↓

[5] 投稿候補一覧の保存
     [1]〜[4]の結果を1つのファイル（CSVまたはJSON）にまとめて保存する。
     ここまでは完全自動。

        ↓

[6] 人間による確認・投稿
     保存された候補一覧を人間が見て、内容と商品画像が合っているか確認したうえで、
     良いと思った紹介文をコピーし、楽天ROOMアプリ／サイトに手動で貼り付けて投稿する。
     投稿し終えたら、その商品IDを data/posted_items.json に追加する
     （これも将来的には「投稿しました」ボタンひとつで自動記録できるようにする想定）。
```

## 4. 開発の進め方（フェーズ分け）

最初から全部を作ろうとすると難しいので、小さく段階を踏みます。

- **フェーズ0（完了）**：設計・調査・最小構成のひな形作成
- **フェーズ1（完了）**：楽天ウェブサービスAPIから実際に商品を取得し、条件判定・重複チェック・
  紹介文生成・候補一覧の保存までを自動で行い、GitHub Actionsから手動実行できるようにした
- **フェーズ2（今後）**：使い勝手の改善（キーワードの追加、除外ワード、生成される文章の質の調整など）
- **フェーズ3（任意）**：投稿済み管理を楽にするための、人間向けの簡単なチェック用画面や記録の仕組み
  （※ここでも「ROOMへの自動投稿」は行いません）

### フェーズ1の実装に関する補足（2026年の楽天API仕様変更について）

楽天ウェブサービスは2026年にAPIの基盤を刷新しました。主な変更点は次のとおりです。

- APIのURL（ドメイン）が `app.rakuten.co.jp` から `openapi.rakuten.co.jp` に変わった
- 認証に「アプリID」だけでなく「アクセスキー」も必要になった
- 古いAPIは順次使えなくなる（すでに使えなくなっている可能性が高い）

このプロジェクトは調査時点で確認できた最新の情報をもとに実装していますが、この作業を行った
ネットワーク環境の制約により、楽天の公式ドキュメントページに直接アクセスして最終確認することは
できませんでした。そのため、実際にGitHub Actionsを実行してみてエラーになる場合は、以下を確認してください。

1. [楽天ウェブサービスの商品検索APIドキュメント](https://webservice.rakuten.co.jp/documentation/ichiba-item-search)
   で最新のエンドポイントURLを確認する
2. `config/settings.yaml`（無ければ`config/settings.example.yaml`をコピーして作成）の
   `api_endpoint` の値を最新のURLに書き換える
3. GitHubのRepository Secretsに登録した `RAKUTEN_APP_ID` ・ `RAKUTEN_ACCESS_KEY` の値が
   楽天ウェブサービスの管理画面に表示されているものと一致しているか確認する

コード自体を変更しなくても設定ファイルの書き換えだけで対応できるようにしてあります。

### HTTP 403エラーが出た件について（追加調査・対応）

実際にGitHub Actionsを実行したところ、認証情報は正しく渡っているにもかかわらずHTTP 403
（アクセス拒否）が返ってくることが分かりました。追加で調べたところ、2026年の刷新後の楽天ウェブサービスは
「ブラウザからのアクセス」を前提にした仕組みになっており、次の対応をしないと403で拒否されることが分かりました。

- リクエストに **Origin** ヘッダーと **Referer** ヘッダーを付け、その値を
  楽天ウェブサービスのアプリ設定にある **「許可されたWebサイト」** に登録したURLと一致させる
- アクセスキー（accessKey）はクエリパラメータだけでなく、**ヘッダー** としても送る

これに対応するため、次のように修正しました。

- `src/rakuten_api.py`：`allowed_origin` を受け取り、Origin/Refererヘッダーとして送信するようにした。
  accessKeyもクエリパラメータとヘッダーの両方に含めるようにした
- `config/settings.example.yaml`：`allowed_origin` の初期値として、以前作成したGitHub Pagesの
  案内ページ（`index.html`）のURLを設定した

**重要**：`allowed_origin` の値は、楽天ウェブサービスのアプリ設定画面で実際に登録した
「アプリケーションURL」「許可されたWebサイト」と**1文字も違わずに一致**している必要があります
（`http`と`https`の違い、末尾のスラッシュの有無でも失敗します）。まだ403が出る場合は、
まずここが完全に一致しているかを確認してください。

エラーメッセージには、楽天から返ってきたエラーの説明文（レスポンス本文）を安全な範囲で表示するように
していますが、万一アプリIDやアクセスキーの値がその中に含まれていた場合に備えて、
表示前に自動的にマスク（伏字）処理をしています。

### スマートフォンでも候補を確認できるようにした対応

当初はActionsの「Artifacts（成果物）」としてZIPファイルを保存する形にしていましたが、
スマートフォンではZIPをダウンロード・展開できない（ストレージ容量の都合など）ことがあるため、
GitHub Actionsの実行結果ページに直接表示される「Summary」欄にも候補一覧を表示するようにしました。

- `src/storage.py`：候補一覧をMarkdownとして組み立てる処理を共通化し、
  件数を絞った要約（`build_summary_markdown`）を作れるようにした
- `src/main.py`：実行の最後に、GitHub Actionsが用意する `GITHUB_STEP_SUMMARY` という
  特別なファイルへ、上位10件（`config/settings.yaml`の`summary_item_limit`で変更可）の
  候補一覧を書き込むようにした。これがそのままActionsの実行結果ページの「Summary」欄に表示される
- 自分のパソコンで実行したときは `GITHUB_STEP_SUMMARY` が存在しないため、この処理は何もしない
  （エラーにもならない）
- 全件のデータはこれまでどおりArtifactsからダウンロードできる状態を維持している

## 5. なぜ「広告対象・ポイント倍率・ランキング入賞・SALE・メディア掲載」を除外理由にしないのか

これらは「商品そのものの良し悪し」とは関係のない、販促上の情報だからです。
今回はあくまで「レビュー評価・レビュー件数・売れ行き・テーマとの関連性」という
客観的な指標で商品を選ぶ設計にしています。

## 6. 商品選定・紹介文の質を上げた対応（31件取得に成功した後の改善）

GitHub Actionsで実際に31件の候補取得に成功したあと、次の点を改善しました。

### 商品選定の改善

- `config/settings.example.yaml` の `keywords` を、検索キーワードと
  「掃除／収納／キッチン／時短／暮らし全般」のカテゴリの組み合わせに変更した
  （紹介文の言い回しをカテゴリごとに変えるために使う）
- `ng_keywords`：「福袋」「ランダム」「訳あり」「詰め合わせ」「アソート」など、
  中身が選べない・用途が不明瞭な商品を弾くための簡易チェックを追加した
  （商品名・商品説明にこれらの言葉が含まれていたら除外する）
- `theme_keywords`：「掃除」「収納」「キッチン」「時短」「便利」など、
  テーマに関連する言葉が商品名・商品説明に1つも含まれない場合は、
  テーマから大きく外れているとみなして除外するようにした
  （※この判定方法には問題が見つかり、後の改善で取りやめている。詳細は「7.」を参照）
- 複数のキーワード検索で同じ商品が重複してヒットした場合、同じ実行の中では
  1回だけ候補に含めるようにした（`dedupe.remove_within_run_duplicates`）
- 最終的な候補は、レビュー件数が多い順（＝購入・利用されている実績が多い順）に
  並べ替えてから上位を表示するようにした

> **重複チェックの限界について（正直な注意点）**：`data/posted_items.json`に記録した
> 「実際に投稿した商品」との重複は確実に除外できます。一方で「過去の実行で候補には出たが
> まだ投稿していない商品」を次回以降も除外する仕組みは、今回は実装していません。
> これを実現するにはGitHub Actionsの実行結果を毎回リポジトリにコミットして残す必要があり、
> 書き込み権限の追加や、現在安定して動いているActionsに影響が出るリスクがあるため、
> 今回は見送りました。必要であれば追加で対応できます。

### 紹介文の質の改善

以前は全商品に対してほぼ同じ定型文（決まった3つの文言）を使い回していましたが、
次のように改善しました。

- カテゴリ（掃除／収納／キッチン／時短／暮らし全般）ごとに、書き出しの文章と
  ポイントの言い回しを複数パターン用意し、商品コードに応じて組み合わせを変える
  ことで、同じ文章が続けて使われにくくした
- 紹介文に含めるポイントのうち1つは「レビュー評価○○・○○件」という、
  実際に取得したデータそのものを使い、確認できない効果や性能は書かないようにした
- ハッシュタグは全商品共通のもの（`#楽天ROOM` `#暮らしの便利グッズ`）に加えて、
  カテゴリに応じたタグ（`#掃除グッズ` `#収納` `#キッチン便利グッズ` `#時短アイテム`）を
  自動で付けるようにした
- 紹介文（書き出し文＋箇条書き2〜4個＋ハッシュタグ）を、改行を含む1つの文章ブロック
  としてまとめ、楽天ROOMの紹介文欄にそのままコピペできる形にした

## 7. 商品選定ロジックのさらなる改善（49件取得後、耳かき等の混入を受けて）

GitHub Actionsで49件の候補取得に成功したあと、上位候補に「耳かき」「パスケース」のような
テーマと関連性が薄い商品や、似た用途の商品（トラベルポーチなど）が複数混ざる問題が見つかったため、
次の改善を行いました。

### テーマとの関連性をさらに強化

- `off_theme_keywords`（新規）：「耳かき」「パスケース」「財布」「ネックレス」など、
  ファッション小物・パーソナルケア用品として明らかにテーマから外れるジャンルの言葉を
  商品名・キャッチコピー・商品説明から検出し、強制的に除外するようにした
- 商品説明（`itemCaption`）も新たに取得し、NGワード・オフテーマワードの判定材料に加えた
  （商品タイトルだけでなく、商品説明も使って判定するため）

> **テストで見つけた問題と対応（正直な注意点）**：当初は「テーマ関連語（掃除・収納など）を
> 1つも含まない商品は除外する」という`theme_keywords`による判定も試しましたが、
> オフラインテストで「コードレス充電式ハンディクリーナー」のような、商品名に
> 『時短』等の単語を含まないだけの正当な時短家電まで誤って除外してしまうことが分かりました。
> 検索キーワード自体がすでにテーマに沿って絞り込まれていることもあり、この判定方法は
> 取りやめ、`off_theme_keywords`による「明らかに違うものだけを弾く」方式に一本化しています。

### 類似商品の統合（新規：`src/ranking.py`）

- 同じカテゴリ（掃除／収納／キッチン／時短／暮らし全般）内で商品名の類似度を比較し、
  類似度が一定以上（初期値0.55、`similarity_threshold`で調整可）の商品は
  「用途がほぼ同じ類似商品」とみなして、レビュー評価・レビュー件数が良い方の1件だけを残す
  （例：似たような「折りたたみ収納ボックス」が3つあれば1つに、掃除・収納など別カテゴリの
  商品同士は比較しないので残る）

### 上位10件のカテゴリ分散（新規：`src/ranking.py`）

- レビュー件数順に並べたあと、同じカテゴリが3件（`summary_max_per_category`で調整可）を
  超えて連続で上位に入らないよう選び直す処理を追加した
- 無理に均等にはせず、候補が少ないカテゴリがあれば他のカテゴリで埋める

### 紹介文をより商品固有の内容に

- 商品名・キャッチコピー・商品説明から「マグネット」「吸盤」「折りたたみ」「水切り」
  「ステンレス」など、商品の構造や仕様に関する言葉を検出し、見つかった場合はその特徴を
  紹介文のポイントに反映するようにした（`description_generator.FEATURE_HINTS`）
- 検出できなかった場合のみ、これまでどおりカテゴリ共通の言い回しで補う
- 実際に使用した体験談のような表現や、商品情報から確認できない効果は引き続き書かない
  （検出した言葉も「〜できるタイプ」「〜しやすい設計」など、断定しすぎない表現にしている）

## 8. 紹介文に商品情報にない特徴が混入していた問題への対応

実際にActionsの上位10件を確認したところ、「スープメーカー」の紹介文に「充電式」、
「詰め替えそのまま MINI」や「段ボールストッカー」の紹介文に「ステンレス製」など、
その商品自体には当てはまらない特徴が混入していることが分かりました。

### 原因

商品説明（`itemCaption`）は、配送案内・付属品の説明・他の対応商品との比較など、
その商品自体の特徴ではない文章を含むことが多いです。たとえば「詰め替えそのまま MINI」の
説明文に「ステンレスボトルにも対応」という一文があった場合、これは「対応するボトルの
一例としてステンレス製もある」という意味であり、商品自体がステンレス製という意味では
ありません。以前の実装は商品説明の全文を対象にキーワードを探していたため、こうした
文脈を無視して単語だけを拾ってしまい、誤った特徴を紹介文に書いてしまっていました。

### 1回目の対応（不十分だった）

- `src/description_generator.py`：特徴語を検出する対象を「商品名」と
  「商品説明の最初の一文だけ」に限定した
- 商品名からもカテゴリを補正する仕組み（`refine_category`）を追加した

この対応後にGitHub Actionsを再実行したところ、実際のSummaryには依然として
同じ問題（スープメーカーに「充電式」、詰め替えそのまま MINI・段ボールストッカーに
「ステンレス」、衣類圧縮袋が掃除カテゴリのまま）が残っていました。

### 実データを直接確認して分かった本当の原因

一時的な診断ログを追加してGitHub Actionsを実行し、実際に楽天ウェブサービスから
返ってきた商品名・商品説明を直接確認したところ、次のことが分かりました。

1. **商品説明が句点（。）を含まない仕様一覧形式のことがある**：
   「詰め替えそのまま MINI」の商品説明は
   `素材/材質ポンプ：エラストマー、ポリアセタール、シリコーンゴム、ステンレス、…`
   のように句点が無い一続きの仕様列挙になっており、「最初の一文」のつもりの処理が
   実質的に説明文全体を拾ってしまっていました
2. **ショップの入力ミスと見られる、無関係な説明文が入っていることがある**：
   「スープメーカー」の商品説明の冒頭は
   `ロングヒット！人気アイテム 片胸・両胸派も！充電式でラクラク搾乳…`
   という、明らかに別商品（搾乳機）の説明文でした。商品名には「充電式」という
   言葉は一切含まれていません
3. **カテゴリ判定が「1語でも一致すれば元のカテゴリのまま」だった**：
   「衣類圧縮袋」の商品名には「掃除機**不要**」という言葉が含まれており、
   この『掃除』の1語だけで「掃除」カテゴリの判定が維持されてしまっていました
   （商品名には「収納」「圧縮袋」「ケース」「ボックス」など、収納カテゴリの
   言葉の方が多く含まれているにもかかわらず）

### 最終的な対応

- `src/description_generator.py`：特徴語の検出を**商品説明を一切使わず、
  商品名だけ**に限定した。商品説明はショップによって書式や品質にばらつきが
  大きく、簡単な文分割では安全に扱えないと判断したため
- `refine_category()`を、単純な「1語でも一致」判定から、**カテゴリごとの
  一致キーワード数を数えて最多のカテゴリを採用するスコア方式**に変更した
  （同点の場合は元のカテゴリを優先）
- 「透明」という特徴語は、収納ケースなどの文脈以外（今回はテープ）にも
  一致してしまい、「中身が見えてわかりやすいタイプ」という表現が不自然になる
  ケースがあったため、検出リストから削除した
- `tests/test_description_generator.py`：**実際にGitHub Actionsの本番実行で
  取得した商品名・商品説明をそのままテストデータとして使用**し、上記の問題が
  再発しないことを確認できるようにした
  （`python -m unittest tests.test_description_generator -v` で実行できる）

なお、この対応は「確実に読み取れる特徴だけを使う」という方向への変更のため、
今まで以上に「特徴が見つからずカテゴリ共通の言い回しになる商品」が増える可能性があります。
これは条件（無理に特徴を作らない）を優先した結果であり、意図した挙動です。

## 9. カテゴリ判定のキーワードを見直し（依然として掃除カテゴリに寄っていた問題）

「8.」の対応後にGitHub Actionsを再実行し、実際のSummaryを確認したところ、
特徴の誤抽出は解消していましたが、カテゴリ判定はまだ不正確でした。

- 「衣類圧縮袋」「詰め替えそのまま MINI」「tower マグネットバスルームフック」が、
  いずれも本来「収納」であるべきところ「掃除」カテゴリ（`#掃除グッズ`）のままだった

### 原因

`refine_category()`のスコア方式自体は正しく動いていましたが、
`CATEGORY_NAME_KEYWORDS`のキーワード選定に問題がありました。

- 「掃除」カテゴリに含めていた**「掃除」という1語そのもの**が広すぎた。
  「掃除機**不要**」（衣類圧縮袋によくある宣伝文句）や「掃除用具」「掃除道具」
  （tower マグネットバスルームフックの商品名にあった、便利グッズ全般を指す
  一般的な表現）にまで一致してしまい、実際には掃除道具ではない商品の
  スコアを底上げしてしまっていました
- 「収納」カテゴリのキーワードに「フック」「ホルダー」「吊り下げ」
  「浮かせる収納」「ポーチ」「収納袋」が含まれておらず、実際には収納用品なのに
  スコアで「収納」が「掃除」に勝てないことがありました

### 対応

- `CATEGORY_NAME_KEYWORDS["掃除"]`から曖昧な「掃除」を削除し、商品自体が
  掃除道具だと言い切れる具体的な言葉（「クリーナー」「モップ」「ワイパー」
  「洗剤」「ブラシ」「雑巾」「掃除機」）だけに絞った
- `CATEGORY_NAME_KEYWORDS["収納"]`に「収納袋」「ポーチ」「フック」「ホルダー」
  「吊り下げ」「浮かせる収納」を追加した
- `NEGATED_PHRASES`（新規）：「掃除機不要」のように、キーワードを含んでいても
  実際には逆の意味を表す言い回しを、判定前に商品名から取り除くようにした
- `tests/test_description_generator.py`：実際にGitHub Actionsで「掃除」カテゴリの
  ままになっていた3商品（衣類圧縮袋・詰め替えそのまま MINI・
  tower マグネットバスルームフック）すべてが「収納」になることを確認するテストと、
  「掃除機不要」を含む商品が掃除カテゴリと判定されないこと、逆に実際の掃除機
  （コードレス掃除機など）は引き続き掃除カテゴリと判定されることを確認する
  テストを追加した（22テストすべて成功）

## 10. 特徴の言い回しをカテゴリに応じて変える（「大容量」が調理家電にも収納向けの表現で出ていた問題）

Run #12のSummaryを確認したところ、カテゴリ判定は改善していましたが、
「ノンフライヤー 4.2L」（カテゴリ：時短）の紹介文に
「たっぷり**収納できる**大容量タイプ」という、収納用品向けの表現がそのまま
使われており、調理家電としては不自然でした。

### 原因

`FEATURE_HINTS`の「大容量」という言い回しは、商品のカテゴリに関わらず
常に同じ「たっぷり収納できる大容量タイプ」という固定文言を使っていました。
「大容量」は収納用品・調理家電・かばん類などジャンルを問わず商品名によく
登場する言葉なので、カテゴリに関係なく同じ言い回しを使うと不自然になる
ケースがありました（同様に「吊り下げ」も「吊り下げて収納できるタイプ」
固定だったため、収納以外のカテゴリで使われると不自然になり得ました）。

### 対応

- `src/description_generator.py`：`FEATURE_HINTS`の値を、固定の文言（文字列）
  だけでなく、カテゴリ名をキーにした言い回しの辞書も指定できるように拡張した
  （`_resolve_hint_phrase`でカテゴリに応じた文言を選ぶ）
- 「大容量」の言い回しをカテゴリ別に用意した：
  収納＝「たっぷり収納できる大容量タイプ」、キッチン＝「一度にたっぷり調理
  しやすい容量」、時短＝「一度にたっぷり使える大容量タイプ」、掃除＝
  「一度にたっぷり集められる大容量タイプ」、それ以外＝「たっぷり使える
  大容量タイプ」
- 「吊り下げ」も同様に、収納以外では「収納」という言葉を使わない
  「吊り下げて使えるタイプ」に変更した
- `tests/test_description_generator.py`：実際の「ノンフライヤー 4.2L」の
  商品名を使い、紹介文に「収納できる大容量タイプ」は出ないこと、
  「収納」という言葉自体が含まれないことを確認するテストを追加した
  （23テストすべて成功）

## 11. 紹介文に絵文字を追加（見やすさ・親しみやすさの向上）

文字だけの紹介文よりも見やすく親しみやすくするため、絵文字を追加した。
このブランチ（`claude/rakuten-room-emoji-descriptions`）は、これまでの
`claude/rakuten-room-automation-dulzvm`ブランチから分岐した新しいブランチで
作業している。

### 方針

- 1投稿あたりの絵文字は3〜6個程度（導入文1個＋箇条書き2〜3個＋レビュー行1個の
  合計4個が基本）
- 導入文の先頭にカテゴリに合った絵文字を1つ（`CATEGORY_EMOJIS`から選ぶ）
- 箇条書きの先頭にも、その内容に合う絵文字を1つずつ付ける
  （`FEATURE_HINTS`・`POINT_VARIANTS`のそれぞれに絵文字を対応付けた）
- レビュー実績の行には`⭐`を固定で使う
- ハッシュタグ行には絵文字を付けない
- 絵文字が内容と無関係に情報を付け足さないよう、絵文字は「すでに文章として
  決まった特徴・カテゴリ」に対する飾りとしてのみ使い、絵文字をきっかけに
  新しい特徴を書き足すことはしない

### 実装

- `src/description_generator.py`：
  - `CATEGORY_EMOJIS`（新規）：カテゴリごとの絵文字候補（例：収納＝
    🧺🧲🧴📦✨、掃除＝🧹🧽✨、キッチン＝🍳🥣🥄✨、時短＝⏱️⚡🍳✨、
    暮らし全般＝🏠✨💡）
  - `POINT_VARIANTS`の各文言に絵文字を対応付け（`(テキスト, 絵文字)`のタプルに変更）
  - `FEATURE_HINTS`の各特徴にも絵文字を対応付け（3要素目として追加。「大容量」
    のようにカテゴリで文言が変わるものは絵文字もカテゴリ別の辞書にした）
  - `_pick_intro_emoji()`（新規）：導入文の絵文字を、箇条書きで使う絵文字と
    被らないようカテゴリの候補からローテーションで選ぶ
    （「同じ絵文字を不自然に連続使用しない」ための配慮）
- `tests/test_description_generator.py`：`EmojiFormatTest`を追加し、
  絵文字の合計数が3〜6個の範囲であること、導入文がカテゴリの絵文字で
  始まること、レビュー行が`⭐`で始まること、ハッシュタグ行に絵文字が
  付かないこと、実際の商品（towerマグネットラック・ノンフライヤー）で
  カテゴリに応じた絵文字が正しく選ばれることを確認した（30テストすべて成功）

既存のカテゴリ判定・特徴抽出・レビュー表記のロジックそのものは変更していない
（絵文字を追加しただけで、判定結果や文言は変わらない）。

## 12. 紹介文を「導入文＋箇条書き」から自然な文章形式に変更

前の11の形式（導入文1行＋箇条書き2〜3行＋レビュー行＋ハッシュタグ行）は、
実際にSummaryで見ると機械的で押し売り感がある、という指摘を受け、
箇条書きを使わない自然な文章形式に作り直した。このブランチ
（`claude/rakuten-room-emoji-descriptions`）の上でそのまま作業している。

### 方針（新しい生成ルール）

- 箇条書き「・」は使わない
- 2〜3文程度の短く読みやすい紹介文にする
- 商品名や特徴に合わせて毎回文章を変える（カテゴリだけで文章を決めない）
- 商品ページ（商品名）から確認できる特徴を自然に文章へ組み込む
- 「〜できそう」「〜に便利そう」「〜したい人におすすめ」など、実際に
  使用していない立場でも不自然にならない表現を使う。「使ってみました」
  「買ってよかった」など、使用したと誤解される表現は使わない
- 絵文字は文末など意味の合う場所に自然に入れる。各行の先頭へ機械的に
  付けない。1投稿につき2〜4個程度、😊✨♪などを使い柔らかい雰囲気にする
- レビュー評価・レビュー件数は紹介文の本文には原則含めない
  （候補一覧には別項目として表示されるため、本文は文章の自然さを優先する）
- ハッシュタグは3〜5個程度、商品に合ったものを自動生成する。
  「#暮らしの便利グッズ」は基本的に入れる。ハッシュタグに絵文字は付けない

### 実装

- `src/description_generator.py`：
  - `FEATURE_HINTS`を`FEATURE_CLAUSES`に置き換えた。値は「タイプです」の
    ような体言止めではなく、「マグネットで浮かせて設置できる」のように
    そのまま名詞（カテゴリ名）の前に続けられる形（連体修飾の節）にした
  - `POINT_VARIANTS`（箇条書き用の断片）を廃止し、代わりに1文目
    （`OPENING_FALLBACK_SENTENCES`・特徴が見つかった場合は`FEATURE_CLAUSES`
    から組み立て）、2文目（`BENEFIT_SENTENCES`、おすすめ・メリットの
    やわらかい言い回し）、3文目（`CLOSING_SENTENCES`、付くこともある
    締めの一文）という、完結した文単位のバリエーションに変更した
  - `generate_description()`：商品コード（無ければ商品名）から作る
    `seed`値で、2文目・3文目の有無・絵文字の選び方をローテーションし、
    同じ商品なら毎回同じ文章になる（再現性がある）が、商品が違えば
    自然にバリエーションが出るようにした
  - `_dedupe_adjacent_emojis()`（新規）：隣り合う文で同じ絵文字が
    続かないよう調整する
  - 1文目には、楽天ROOMでよく見る「文末に絵文字を2つ重ねる」表現
    （例：🛁✨）を一定の確率で加えている。追加する絵文字は、その行だけで
    なく紹介文全体で既に使われている絵文字を除外して選ぶ（開発中に、
    他の文の絵文字と重複してしまう不具合が見つかったため、文章全体で
    重複を避けるよう修正した）
  - レビュー評価・件数を本文に含める処理（`REVIEW_EMOJI`・レビュー行の
    組み立て）を削除した
  - `HASHTAG_BY_CATEGORY`の「暮らし全般」を、基本ハッシュタグの
    「#暮らしの便利グッズ」と重複しない「#暮らし雑貨」に変更した
    （重複していると、サブトピックに一致しない商品でハッシュタグが
    2個しか付かず、「3〜5個程度」のルールを満たせなくなるため）
- `config/settings.example.yaml` / `src/main.py`：紹介文の例に
  「#楽天ROOM」が含まれていなかったため、`default_hashtags`から
  「#楽天ROOM」を外し「#暮らしの便利グッズ」のみにした
- `tests/test_description_generator.py`：箇条書き・レビュー行を前提にした
  アサーションをすべて書き換え、新しいルール（箇条書きなし、文の数が
  2〜3、絵文字数が2〜4、レビュー数値が本文に含まれない、ハッシュタグ数が
  3〜5）を確認するテストに変更した。既存の実データ回帰テスト（衣類圧縮袋・
  詰め替えそのままMINI・towerマグネットラック／フック・ノンフライヤー等）
  はそのまま流用し、新しい文章形式でも同じ問題が再発しないことを確認した。
  また、多数の商品名・全カテゴリの組み合わせでフォーマットのルールが
  常に守られることを確認する一括テストも追加した（37テストすべて成功）

カテゴリ判定（`refine_category`）・特徴の検出対象を商品名だけに限定する
ロジックは変更していない。

## 13. 商品名の単語だけで特徴を決めつけない対応（用途と食い違う紹介文の修正）

「12.」の対応後にGitHub Actionsを実行し、実際のSummaryを確認したところ、
文章の形式は改善していたが、次のように「商品名に含まれる単語」と
「商品全体の用途」が食い違う紹介文が見つかった。

- 三角コーナー（生ゴミの水切り用）の商品名に「吸盤」が含まれ、
  紹介文が「吸盤で好きな場所に取り付けられる」になっていた
- 味噌マドラーの商品名に「ステンレス」が含まれ、紹介文が
  「サビに強いキッチングッズです」になっていた
- ドアストッパーの商品名に「マグネット」が含まれ、紹介文が
  「マグネットで浮かせて設置できる」（収納用品向けの表現）になっていた

### 原因

`FEATURE_CLAUSES`（構造・仕様の単語一致）で見つかった特徴を、そのまま
1文目（紹介文の主役）にしていたことが原因。楽天の商品名はSEO対策で
多くの単語が詰め込まれていることが多く、「単語が商品名に含まれる」ことは
「それが商品全体の用途である」ことを意味しない。特に「マグネット」
「吸盤」「ステンレス」のような構造・素材の単語は、商品の主な用途とは
無関係な付属的な特徴を指していることがある。

### 対応

- `src/description_generator.py`：
  - `PRODUCT_TYPE_HINTS`（新規）：商品名から「この商品が具体的に何であるか」
    がほぼ確実に分かる場合に使う、商品全体の用途に沿った1文目の一覧。
    `FEATURE_CLAUSES`（構造・仕様の単語一致）より必ず先に判定する
    （三角コーナー・ドアストッパー・マドラーを追加）
  - 1文目（`_build_sentence1`）は、`PRODUCT_TYPE_HINTS`に一致しなければ
    必ず`OPENING_FALLBACK_SENTENCES`（カテゴリ共通の用途ベースの言い回し）
    を使うようにし、`FEATURE_CLAUSES`から1文目を組み立てる処理を廃止した
    （構造・仕様の単語だけを根拠に1文目を作らないようにするため）
  - `FEATURE_CLAUSES`で見つかった特徴は、`PRODUCT_TYPE_HINTS`で商品の種類が
    特定できなかった場合に限り、2文目に「〜なので、」という**理由**として
    添えるだけにした（`_build_sentence2`）。特徴を紹介文から無くすのではなく、
    「主役にしすぎない」形で残している
  - `FEATURE_CLAUSES`から「ステンレス→サビに強い」を削除した。素材だけを
    述べる表現は、商品全体の用途（使うと何がラクになるか）に繋がりにくく、
    紹介文の主題として不自然になりやすいため
  - 「吸盤」の絵文字を、磁石を連想させる🧲から、汎用的な✨に変更した
    （吸盤は磁石ではないため）
  - `BENEFIT_SENTENCES`・`CLOSING_SENTENCES`のバリエーションを各2個ずつ
    追加し、同じ定型文が連続しにくくした
- `tests/test_description_generator.py`：`ProductTypeMismatchRegressionTest`
  （新規）を追加し、三角コーナー・味噌マドラー・ドアストッパーを
  実際の商品名によく見られる形（色々な単語が詰め込まれた商品名）で再現し、
  商品全体の用途に沿った文章になること、「ステンレス→サビに強い」が
  `FEATURE_CLAUSES`から削除されていること、1文目が構造・仕様の単語だけで
  決まらないこと、特徴は2文目の理由としてのみ現れることを確認した
  （43テストすべて成功）

カテゴリ判定（`refine_category`）のロジック、絵文字の個数・配置ルール、
ハッシュタグの個数ルールは変更していない。

## 14. 紹介文を「共感型」の6ブロック構成に変更

「13.」の対応後にGitHub Actionsを実行し、実際のSummaryを確認したところ、
商品との不一致は改善していたが、文章が抽象的すぎる（「気になる場面で活躍
してくれそうです」等、商品を見なくても使える文章）という指摘を受けた。
一度その対応に着手したが、最終的には方向性そのものを変更し、楽天ROOMで
読んだ人が「これ便利そう」「欲しい」と感じやすい、共感型の紹介文に
作り直した。

### 新しい構成

1文〜2文の短い文章ではなく、次の6ブロック構成にした（各ブロックは空行で区切る）。

```
① 絵文字＋短いキャッチコピー（例：🏠 リモコンの置き場所、決まってる？✨）
② 使う前にありがちな悩み・あるある（1〜2文、「〜って意外と面倒…😅」等）
③ 商品がどう解決してくれそうか（例：そんな小さなストレスを減らしてくれそうな収納ラック◎）
④ ✔️を使ったメリット3項目（✔️ リモコンの定位置を作れる 等）
⑤ 自然な一言の締め（商品ごとに複数パターンから選ぶ）
⑥ ハッシュタグ3〜5個
```

### 実装

- `src/description_generator.py`：
  - `_PostTemplate`（新規）：①〜⑤の文章一式（キャッチコピー・絵文字・悩み・
    解決・メリット2項目・締めの複数パターン）を持つ入れ物
  - `PRODUCT_TYPE_TEMPLATES`（`PRODUCT_TYPE_HINTS`を全面的に置き換え）：
    商品名から具体的な商品タイプが分かる場合に使う、①〜⑤すべてを含む
    テンプレート一覧。ロボット掃除機・スープメーカー・ノンフライヤー・
    フライパン・リモコンラック・水切りボウル・シーツハンガー・
    ドライヤースタンド等、よくある商品タイプを収録した
  - `GENERIC_TEMPLATES`（`OPENING_FALLBACK_SENTENCES`・`BENEFIT_SENTENCES`・
    `CLOSING_SENTENCES`を統合・置き換え）：商品タイプが特定できない場合に
    使う、カテゴリ（掃除／収納／キッチン／時短／暮らし全般）共通の
    安全なテンプレート
  - ✔️メリットの3項目のうち、2項目はテンプレートの安全な内容を使い、
    残り1項目は商品名から`FEATURE_CLAUSES`（マグネット式・折りたたみ式等）
    が読み取れればそれを使う（読み取れなければテンプレートの安全な
    言い回しで補う）。これにより、「13.」で対応した「商品名の単語だけで
    決めつけない」考え方（商品全体の用途を主役にし、構造・仕様の特徴は
    補足として添える）を新しい構成でも引き継いでいる
  - `LOCATION_HINTS`・`_resolve_location()`は維持し、`GENERIC_TEMPLATES`の
    「収納」テンプレートでは`{location}`のプレースホルダーを使って、
    商品を使う場所（浴室・キッチン・玄関等）が分かればその言葉に、
    分からなければ「身の回り」に置き換わるようにした
  - `LOCATION_TOPIC_EMOJI`（新規）：場所が分かる場合、①の先頭の絵文字も
    場所に合わせて変える（浴室用品なら🛁、洗面所用品なら🪞等）
  - レビュー評価・件数に加えて、**価格**も紹介文に含めないようにした
    （今回追加されたルール）
  - `CATEGORY_EMOJIS`・`_dedupe_adjacent_emojis`等、絵文字をランダムに
    ローテーションする仕組みは不要になったため削除した（✨😅◎✔️☺️は
    構成上固定の飾りとして使い、先頭の絵文字だけが商品タイプ・場所に
    応じて変わる、という単純な仕組みにした）
- `tests/test_description_generator.py`：6ブロック構成（①〜⑥それぞれの
  始まり・終わり方）を確認するテストに全面的に書き換えた。価格が本文に
  含まれないこと、商品名をそのまま長く転載しないことを確認するテストを
  新たに追加し、既存の実データ回帰テスト（衣類圧縮袋・詰め替えそのまま
  MINI・towerマグネットラック／フック・スープメーカー・ノンフライヤー等）
  もこの構成に合わせて更新した（44テストすべて成功）

カテゴリ判定（`refine_category`）・特徴の検出対象を商品名だけに限定する
ロジック・ハッシュタグの組み立てロジックは変更していない。

## 15. 「確認できない性能を推測しない」ルールの強化

「14.」の6ブロック構成後にGitHub Actionsを実行し、実際のSummaryを確認した
ところ、文章の雰囲気・構成は理想に近かったが、次のように商品名・取得データ
からは確認できない性能・用途・操作方法を推測してしまっている例が見つかった。

- ドアストッパー：「マグネットで浮かせて設置できる」→マグネットで
  取り付けられることは確認できるが、「浮かせて設置」は収納用品向けの
  用途で、ドアストッパーの用途と合わない
- ノンフライヤー：「油を使わずに揚げ物を作れる」→「油不使用」と明示
  されていないのに、油ゼロを断定していた
- フライパン：「焦げつきやすいフライパンだと」「後片付けしやすい」→
  商品情報に明示されていない性能を悩み・メリットとして書いていた
- ロボット掃除機：「スイッチひとつで」→商品名・データに明示されていない
  操作方法を勝手に限定していた

### 対応

- `src/description_generator.py`：
  - `FEATURE_CLAUSES`の「マグネット」の文言を「マグネットで浮かせて設置
    できる」から、商品名から確認できる事実だけの「マグネットで取り付け
    られる」に変更した。これは✔️メリットの3項目目として、ドアストッパー・
    収納ラックなど商品タイプを問わず使われるため、1箇所の修正で
    全体に反映される
  - `PRODUCT_TYPE_TEMPLATES`の該当箇所を修正：
    - ノンフライヤー：「油を使わずに」→「油を控えて」に変更し、
      「揚げ物風の調理がしやすい」「油の後片付けを減らしやすい」という、
      商品情報から安全に言える表現にした
    - フライパン：「焦げつきやすいフライパンだと」という悩みを、
      商品固有の性能を前提にしない「毎日使うものだから、扱いやすさや
      使い勝手が気になりますよね」という一般的な悩みに変更した。
      「後片付けもしやすくなりそう」も「毎日の調理に取り入れやすそう」
      に変更した
    - ロボット掃除機：「スイッチひとつで床掃除をおまかせできる」から
      「スイッチひとつで」という操作方法の限定を外し、「床掃除を
      おまかせしやすい」に変更した
  - 上記以外のテンプレート・`GENERIC_TEMPLATES`・`FEATURE_CLAUSES`
    （大容量・充電式等）についても、「できる」という言い切りの表現を
    「できそう」「しやすい」といった、ためらいのある言い回しに揃える
    軽い見直しを行った（断定が危険な場合はやわらかい表現にする、という
    ルールの徹底）
  - モジュールの docstring（生成ルール）に「商品名、APIから取得した情報、
    明示された仕様だけを根拠にする」「悩み（②）も、商品から自然に想定
    できる一般的な悩みだけにする」という条件を明記した
- `tests/test_description_generator.py`：報告された4パターン
  （ドアストッパーのマグネット文言、ノンフライヤーの油ゼロ断定、
  フライパンの未確認性能、ロボット掃除機の操作方法限定）それぞれについて、
  該当する誤った表現が紹介文に出てこないことを確認する回帰テストを追加した
  （47テストすべて成功）

共感型の6ブロック構成・現在のトーン、ハッシュタグ3〜5個のルールは
変更していない。

## 16. 毎日「日本時間15:30」の固定時刻に自動実行する仕組み

GitHub Actionsを毎日自動実行する仕組みは、当初「日本時間19:30〜21:00の間の
ランダムな時刻（最初は1分単位、後に5分単位）」として実装した（詳しい経緯は
このリポジトリのコミット履歴を参照）。その後、「もっとシンプルな、毎日
固定の時刻（日本時間15:30）に実行する方式に変更したい」という要望を受け、
ランダム時刻を決める仕組み（`schedule_next_run.yml`・`src/scheduler.py`・
`tests/test_scheduler.py`・`data/schedule/schedule_log.json`）はすべて削除し、
`run_scheduled_search.yml`だけを残して、毎日決まった時刻に
`search_candidates.yml`を起動するだけのシンプルな構成にした。

### 現在の仕組み

`.github/workflows/run_scheduled_search.yml`が、
`cron: "30 6 * * *"`（UTC 06:30 = 日本時間15:30）で毎日1回起動し、
`gh workflow run search_candidates.yml`で既存の商品候補検索ワークフローを
起動するだけ、というシンプルな構成になっている。

```
[毎日 日本時間15:30 (UTC 06:30)]
run_scheduled_search.yml
        │
        │ gh workflow run
        ▼
search_candidates.yml（既存の商品候補検索・変更なし）
```

- cronは1日1回しか予定時刻に一致しないため、同じ日に自動実行が重複することは
  仕組み上ない（ランダム時刻の方式で必要だった「発火済みフラグ」のような
  状態管理は不要になった）。
- sleepや、時刻を事前に決めて記録しておく仕組みは使っていない。
- `workflow_dispatch`による手動実行は今までどおり即座に実行できる
  （`run_scheduled_search.yml`・`search_candidates.yml`のどちらも）。
- `search_candidates.yml`（既存の商品候補検索ワークフロー）・
  紹介文生成（`description_generator.py`）・GitHub Repository Secrets
  （`RAKUTEN_APP_ID`・`RAKUTEN_ACCESS_KEY`）は変更していない。

### 前提条件（実際に動かす前の確認事項）

`run_scheduled_search.yml`が`search_candidates.yml`をAPI経由で起動するには、
GitHub Actionsの既定の`GITHUB_TOKEN`が「Actions」への書き込み権限
（`permissions: actions: write`）を持てる必要がある。リポジトリの
Settings → Actions → General → Workflow permissionsが「Read and write
permissions」になっていることを確認してほしい（読み取り専用に設定されている
場合、`search_candidates.yml`の自動起動が失敗する）。

## 17. スマホから楽天ROOMへ投稿しやすい専用ページ（`room/`）

毎日の検索結果（上位10件）を、Artifactのダウンロードや候補一覧ファイルを
開かなくても、スマートフォンのSafariから固定URLを開くだけで確認・
コピー・リンクを開けるようにするための専用ページを追加した。

**URL: <https://9892stpvvp-png.github.io/-rakuten-room-auto/room/>**

既存のGitHub Pages（リポジトリ直下の`index.html`、楽天ウェブサービスの
「アプリケーションURL」「許可されたWebサイト」として登録しているページ）は
一切変更していない。今回追加した`room/index.html`はその下の別パス
（`/room/`）に置いているだけなので、登録済みのURL
（`https://9892stpvvp-png.github.io/-rakuten-room-auto/`）には影響しない。

### 仕組み

商品検索・条件判定・重複チェック・紹介文生成（`src/main.py`以下）は
一切変更していない。`search_candidates.yml`の既存ステップ（検索・Artifact保存）
の後ろに、次の2ステップを追加しただけ。

1. `python -m src.publish_room_page`：`src/main.py`がすでに`data/candidates/`
   に保存した最新の候補一覧（`candidates_*.json`、すでに上位10件に絞り込み
   済み）を読み込み、投稿ページに必要な項目（商品名・価格・レビュー評価/
   件数・商品URL・画像URL・紹介文・生成日時）だけを取り出して
   `room/data/candidates.json`に書き出す。紹介文は一切加工しない。
2. 変化があれば`room/data/candidates.json`をコミット・プッシュする
   （変化がなければ何もしない）。GitHub Pagesは対象ブランチへのpushで
   自動的に再公開されるため、これだけでページの内容が更新される。

ページ側（`room/index.html`）は、開かれるたびに`data/candidates.json`を
`fetch`で読み込んで表示するだけの静的ページ。前日以前のデータは
`room/data/candidates.json`が**上書き**されるため混ざらない（追記ではない）。

### 楽天ROOM投稿ボタンのリンク先について（重要・正直な注意点）

「商品URLを渡して直接ROOMの投稿画面に遷移できる、公式で安定したURL仕様」は
引き続き見当たらない。調査で分かったのは次の点。

- 楽天ROOMの公式ヘルプ・ガイドによれば、商品をROOMに投稿する公式な方法は
  （a）楽天市場の商品ページ上に用意されている「ROOMに投稿する」を使う、
  （b）ROOMアプリ／サイト内の検索で商品名を検索して投稿する、の2通りで、
  いずれもROOM側のUI操作が必要であり、URLのクエリパラメータで商品を指定して
  投稿画面に直接遷移する、というURL仕様は見当たらなかった
  （参考：[どうやって投稿するの？｜基本的な使い方 - ROOM](https://room.rakuten.co.jp/info/guide/utilization/02how_room/02.html)）。
- 上記（a）が公式に案内されている以上、ROOMのトップページを経由して
  「＋投稿→キーワード検索→商品検索→投稿」と複数操作させるより、楽天市場の
  商品ページへ直接遷移させて、その場にある公式の「ROOMで投稿」を
  使ってもらう方が確実で最短の導線になる。そのため「② 楽天市場を開く」
  ボタンは、APIから取得した正式な商品URL（`item_url`）をそのまま開く仕様に
  変更した（以前使っていたROOMトップページ`https://room.rakuten.co.jp/`は
  開かなくなった）。商品URLはAPIレスポンスをそのまま使っており、推測や
  加工は一切行っていない。ボタンの下には「楽天市場の商品ページで
  『ROOMで投稿』を押す」という案内を表示している（実機のiPhone操作で、
  ①紹介文をコピー→②楽天市場を開く→商品ページの「ROOMで投稿」→ROOMの
  商品投稿画面→紹介欄へ貼り付け→投稿、という最短ルートが正常に動作する
  ことを確認済み）。
- 非公式なURLスキーム（アプリ専用の`rakuten-room://`等）・非公式ディープ
  リンクは一切使用していない。
- 商品ページ側に「ROOMで投稿」が表示されない場合（レイアウトや商品の
  種類によって表示されないことがある）に備え、予備手段として「ROOM検索
  ワードをコピー」ボタンを残している。商品名をクリップボードにコピーし、
  ROOM内の検索で貼り付けて投稿する、という従来の手順に戻れるようにしている。
- 従来あった「楽天商品ページを開く」ボタンは、上記の「② 楽天市場を開く」
  ボタンと開き先（`item_url`）が完全に同じため、重複を避けて統合し廃止した。

### 投稿済み管理

「投稿済みにする」は、この端末のブラウザの`localStorage`
（キー: `rakutenRoomPostedItems`、値: `{商品コード: true, ...}`）に
保存するだけで、GitHubへの書き戻しや楽天側への送信は一切行わない。
別の端末やブラウザには引き継がれない。

### 安全性

楽天ID・パスワード・Cookie・セッション情報は一切保存・送信しない。
ROOMへの自動投稿は実装しておらず、各ボタンは公式ページ／自分のクリップ
ボードを開く・操作するところまでにとどめている。既存のGitHub Repository
Secrets（`RAKUTEN_APP_ID`・`RAKUTEN_ACCESS_KEY`）には一切触れていない。

## 18. 毎日の候補10件を「暮らしの便利グッズ5件」＋「消耗品・飲料5件」に分ける

これまで毎日の投稿候補10件は、レビュー実績順に並べたうえでカテゴリの偏りだけを
抑える1つの枠から選んでいた。今回、暮らしの便利グッズだけでなく、洗剤・
キッチン消耗品・トイレットペーパーなどの日用品や、水・お茶・ジュースといった
飲料も投稿できるようにするため、選定を「暮らしの便利グッズ」枠と
「消耗品・飲料」枠の2つに分け、それぞれ5件ずつ（合計最大10件）選ぶ方式に変更した。

### 仕組み

- `config/settings.yaml`の`keywords`の各エントリに`group`
  （`convenience`=暮らしの便利グッズ／`consumable`=消耗品・飲料）を追加した。
  既存の5キーワード（掃除・収納・キッチン・時短・暮らし全般）は
  `group: convenience`のまま、内容・検索ロジックは変更していない。
  新たに12キーワード（洗剤・キッチン消耗品・日用品・水・お茶・ジュースの
  各2件）を`group: consumable`として追加した。
- `src/main.py`のフェーズ1（検索・条件判定・重複チェック）は、
  レビュー条件・NGワード・テーマ除外・重複チェックのロジック自体は
  一切変更せず、各商品に`_group`（枠）と`_display_group`
  （投稿ページに表示する「便利グッズ／消耗品／飲料」の小さな表示）を
  タグ付けするようにしただけ。「消耗品・飲料」枠は検索キーワード自体が
  商品種別そのもの（例：「洗濯洗剤」というキーワードで検索した商品は
  確実に洗剤）なので、便利グッズ枠のような商品名からのカテゴリ補正
  （`refine_category`）は行わない。補正ロジックのキーワード一覧は
  便利グッズ向けに作られており、そのまま適用すると例えば洗剤の商品名に
  ある「洗剤」の一語で掃除カテゴリに誤って補正されてしまうため。
- 飲料カテゴリ（水／お茶／ジュース）の商品だけ、`ng_keywords`等とは別に
  新設した`alcohol_keywords`（ビール・チューハイ・ワイン等）で
  アルコール飲料を除外する（`filters.filter_by_alcohol_keywords`）。
- 飲料カテゴリの商品は、`ranking.sort_consumable_items`で「24本」
  「ケース」「まとめ買い」のような複数本セットを示す表記
  （`ranking.is_multipack`）を単品より優先して並べる。飲料以外の
  消耗品（洗剤・キッチン消耗品・日用品）は、これまでどおりレビュー実績
  だけで並べる。
- フェーズ2（類似商品の統合）・フェーズ3（紹介文生成）は既存のロジックの
  ままで、`_group`によって処理を分けていない（カテゴリが便利グッズ・
  消耗品・飲料の間で重複しないため、これまでの仕組みがそのまま使える）。
- フェーズ4（上位候補の選定）だけを、`ranking.sort_by_quality` +
  `ranking.diversify_top`の単純な呼び出しから、新設の
  `ranking.select_balanced_top`に差し替えた。「暮らしの便利グッズ」枠・
  「消耗品・飲料」枠それぞれで`diversify_top`（カテゴリの偏り防止つき）を
  実行して上位5件を選び、どちらかの枠の総候補数が5件に満たない場合だけ、
  その不足分をもう片方の枠の残り候補（品質順）から補充する。合計は
  常に10件（`convenience_target + consumable_target`）を超えない。

### 紹介文

`src/description_generator.py`の`GENERIC_TEMPLATES`に、消耗品・飲料向けの
6カテゴリ（洗剤・キッチン消耗品・日用品・水・お茶・ジュース）を追加した。
「ストックが切れると困る／切らしたくない」という消耗品ならではの悩みを
①〜③に反映しつつ、既存カテゴリと同じ6ブロック構成・断定を避ける言い回し・
「絶対お得」「必ず安い」「健康になる」等の根拠のない表現を使わないルールは
そのまま踏襲している（価格・レビュー評価・件数を本文に含めないルールも同じ）。

### スマホ投稿ページ（`room/`）への反映

`src/publish_room_page.py`が書き出すJSONに、各商品の`group_label`
（「便利グッズ」「消耗品」「飲料」）と、内訳件数`convenience_count`・
`consumable_count`を追加した。`room/index.html`側では、各カードに
小さなカテゴリバッジを表示し、進捗表示の下に「内訳：便利グッズ5件 /
消耗品・飲料5件」を表示するようにした。紹介文コピー・商品URLコピー・
楽天商品ページ／楽天ROOMボタン・投稿済み管理・未投稿のみ表示・進捗表示の
仕組み自体は変更していない。

## 19. 投稿済み履歴による重複防止の強化

これまでの重複チェックは、`data/posted_items.json`の`posted_item_codes`
（商品コードだけの一覧）と、商品コードが完全一致するかどうかだけで
判定していた。今回、投稿済み履歴をより詳しい情報（商品URL・商品名・投稿日時
・カテゴリ）を持つ形に拡張し、商品コードが取れない・変わってしまった場合でも
商品URLで一致すれば重複とみなせるようにした。あわせて、スマホ投稿ページで
「投稿済みにする」にした商品を、履歴へ取り込みやすい形でコピーできるように
した。

### 履歴ファイルの形式（後方互換）

```json
{
  "posted_item_codes": ["shop:item001", ...],
  "posted_items": [
    {
      "item_code": "shop:item001",
      "item_url": "https://item.rakuten.co.jp/shop/item001/",
      "product_name": "...",
      "posted_at": "2026-09-14T06:30:00+00:00",
      "category": "収納"
    },
    ...
  ]
}
```

`posted_item_codes`は後方互換のために残してあり、`posted_items`から
自動的に再計算される（`src/dedupe.py`の`_save_posted_items`）。以前の
形式（`posted_item_codes`のみのファイル）もそのまま読み込める
（`load_posted_items`が`item_code`だけの簡易レコードに変換する）ため、
既存のファイル・過去のGitHub Actions実行との互換性は壊れていない。

### 重複判定の優先順位

`src/dedupe.py`の`is_posted`が、次の順で商品が投稿済みかどうかを判定する。

1. `item_code`（安定した商品ID）が履歴に登録されている商品コードと一致するか
2. 商品URLを正規化（クエリ文字列・フラグメント・末尾スラッシュの違いを
   無視）した上で、履歴に登録されている商品URLと一致するか

`src/main.py`のフェーズ1（キーワードごとの検索直後、既存のレビュー条件・
NGワード・テーマ除外・アルコール除外・耐久アクセサリ除外と同じ並びの
タイミング）でこの判定を使って候補から除外する。除外は選定（フェーズ4の
`select_balanced_top`）より前に行われるため、既存の「片方の枠が5件に
満たない場合はもう片方から補充する」という仕組みがそのまま働き、投稿済み
商品を除外したあとも可能な範囲で合計10件・5+5のバランスを維持できる
（`select_balanced_top`自体は変更していない）。

### スマホ投稿ページの「投稿済みデータをコピー」

GitHub Pagesは静的サイトのため、ブラウザからGitHubリポジトリへ直接
書き戻す仕組みは（意図的に）追加していない。代わりに、「投稿済みにする」で
登録した商品の情報（商品コード・商品URL・商品名・投稿日時）をlocalStorage
にそのまま保持しておき、ページ上部の「投稿済みデータをコピー」ボタンで
`data/posted_items.json`にそのまま取り込める形式のJSONとしてクリップボードに
コピーできるようにした。以前のバージョン（真偽値のみ保存）で登録された
商品が残っていても、表示中の候補データから商品URL・商品名を補って
コピーできるようにしている（後方互換）。

### 取り込み用の補助スクリプト・関数分離

コピーしたJSON（またはそれ以外の方法で用意した、過去の投稿済み商品一覧）を
履歴に取り込むため、`python -m src.import_posted_items <ファイル>`を追加した。
履歴への書き込み処理自体は`dedupe.append_posted_items`という1つの関数に
分離してあり、`import_posted_items`はその薄いCLIラッパーに過ぎない。
将来、別の手段（ROOM側の何らかの正式なエクスポート機能等）で履歴を
自動更新できるようになった場合も、この関数を呼び出すだけで済む設計に
している。`append_posted_items`は、履歴・追加しようとしているリスト自身の
両方について、商品コードまたは商品URLの重複を確認してから追記するため、
同じ商品を誤って二重登録することはない。

このスクリプトでは楽天ROOMへのログイン・Cookie・セッション情報を使った
自動取得・自動投稿は一切行っていない。あくまで、人間が用意した（またはROOMの
公式画面からコピーした）商品一覧を取り込むだけの処理である。

### GitHub Actions Summaryへの表示

毎日の実行結果に、投稿済み履歴による重複防止の状況
（`storage.build_posted_history_summary_markdown`）を追加した。

- 投稿済み履歴によって除外した件数
- 今回選ばれた新規候補の件数
- 現在の投稿済み履歴の総数

これらは、既存の候補一覧表示（`storage.build_summary_markdown`）の
直前に表示される。既存のSummary表示・Artifact保存の仕組みは変更していない。

## 20. item_code・URLが無い過去投稿のための、商品名ベースの補助重複判定

楽天ROOMへの過去の投稿（81件）を重複防止に活かしたいが、投稿時点の
`item_code`や商品URLを記録していない場合がある。そこで、`item_code`・
商品URLでの判定（既存・変更なし）に加えて、商品名の正規化後の完全一致による
補助的な重複判定を追加した。優先順位は次のとおり（`dedupe.match_posted_reason`）。

1. `item_code`が一致するか
2. 正規化した商品URLが一致するか（既存の判定、変更なし）
3. 正規化した商品名が一致するか（今回追加。1・2より優先度を下げてある）

### 商品名の正規化（`dedupe.normalize_product_name`）

次の処理を順に行う。

1. Unicode正規化（NFKC）——全角/半角の違い（英数字・記号・スペース）を
   吸収する。
2. 絵文字の除去（代表的な絵文字のUnicode範囲を対象にした簡易的な除去）。
3. 販売文言・宣伝表現の除去——「送料無料」「ポイント◯倍」「ランキング」
   「公式」「限定」「レビュー特典」「ギフト」「SNSで(も)話題」「365日発送」
   「クーポン」「セール」を、指定された言葉・パターンだけ取り除く（依頼に
   挙げられた例のとおりで、網羅的な広告文言辞書は作っていない）。
4. 装飾記号（【】《》〈〉「」『』［］()（）!！?？・*＊#＃★☆）の除去——
   記号「だけ」を取り除き、**中身の文字列は消さない**（例：「《 山崎実業
   マグネットバスルームラック タワー ワイド 》」→「 山崎実業
   マグネットバスルームラック タワー ワイド 」）。
5. 英字の小文字化。
6. 連続する空白の1つのスペースへの圧縮・前後の空白除去。

ブランド名・シリーズ名・サイズ・容量・本数・個数・型番・S/M/L・
ワイド/ラージ・◯個セットなどは、上記のどの処理でも対象にしていないため、
そのまま残る。

### 誤判定防止（あいまい一致にしない）

`match_posted_reason`での商品名判定は、正規化後の文字列が**完全一致**した
場合だけ「投稿済み」とみなす。部分一致・類似度判定（`ranking.py`の
`deduplicate_similar_items`が使っている`difflib.SequenceMatcher`）は
使っていない。理由は、似ている別商品を誤って除外しないため。

- 「tower マグネットバスルームラック タワー ワイド」と
  「tower マグネットバスルームラック タワー ラージ」→ 正規化後も末尾の
  「ワイド」「ラージ」が異なるため一致せず、別商品として扱われる。
- 「冷凍ごはん容器 1個」と「冷凍ごはん容器 5個セット」→ 同様に「1個」
  「5個セット」が異なるため一致せず、別商品として扱われる。

また、正規化した結果が空文字になった商品名同士（絵文字だけの商品名等）を
誤って「両方空文字だから一致」としないよう、空文字は常に不一致として扱う。

### 過去投稿の初期登録

`item_code`・商品URLが分からない過去投稿も、商品名だけで
`data/posted_items.json`に登録できるようにした（`dedupe.append_posted_items`
は、`item_code`・商品URL・商品名のいずれか1つでもあれば受け付ける。すべて
無い場合だけ登録を拒否する）。

```json
{
  "product_name": "マーナ シートケース",
  "item_code": null,
  "item_url": null,
  "posted_at": null,
  "category": "過去投稿"
}
```

`item_code`・商品URL・`posted_at`に`null`が入っていても、空文字として
安全に扱う（`dedupe._clean_str`）。取り込みは`src/import_posted_items.py`
（変更なし、既存の一括取り込みスクリプトがそのまま使える）。

過去投稿をまとめて登録するための下書き用として、`data/past_posted_items_seed.json`
を新設した。このファイル自体は検索時の本番履歴（`data/posted_items.json`）
としては読み込まれない。あくまで`import_posted_items`で取り込むための
一時置き場で、商品名の一覧を追記してから

```bash
python -m src.import_posted_items data/past_posted_items_seed.json
```

を実行すると、`data/posted_items.json`へ安全に（重複を避けて）取り込まれる。

### GitHub Actions Summaryの内訳表示

`storage.build_posted_history_summary_markdown`に、`item_code`・URL・
商品名のそれぞれで何件除外したかの内訳を追加した。

- item_codeによる除外件数
- URLによる除外件数
- 商品名履歴による除外件数

既存の「投稿済み履歴によって除外した件数」（3つの合計）・「今回選ばれた
新規候補」・「現在の投稿済み履歴の総数」の表示は残したままにしている。

便利グッズ5件＋消耗品/飲料5件・レビュー条件・既存のitem_code/URL重複判定・
紹介文生成・毎日15:30 JST自動実行・スマホ投稿ページ・投稿済みデータコピー・
GitHub Pages・Secretsは変更していない。

## 21. スクリーンショットからしか確認できない過去投稿のための、match_keywords補助判定

過去のROOM投稿81件の中には、商品名すら正確に思い出せず、スクリーンショットの
一部（ブランド名や特徴的な言葉）からしか特定できないものがある。そこで、
既存の判定（item_code→URL→商品名の完全一致、変更なし）に加えて、4番目の
補助判定として`match_keywords`を追加した。

### 優先順位

`dedupe.match_posted_reason`の判定順位は次のとおり（1〜3は変更なし）。

1. `item_code`が一致するか
2. 正規化した商品URLが一致するか
3. 正規化した商品名が完全一致するか
4. `match_keywords`（登録した特徴語）がすべて商品名に含まれるか（今回追加。
   他の3つより優先度を下げてある）

### match_keywordsの判定（`dedupe.match_all_keywords`）

候補の商品名・登録したキーワードの両方をNFKC正規化・小文字化した上で、
登録された`match_keywords`の**全語**が商品名に含まれている場合だけ一致と
みなす（AND判定）。`normalize_product_name`（3の判定で使う正規化）と違い、
販売文言・装飾記号の除去は行わない——キーワード自体を人間が選んでいるため、
広告文言が紛れ込む心配がなく、余計な処理をしない方が誤判定のリスクが低いと
判断した。

```python
match_all_keywords("tower 《 山崎実業 マグネットバスルームラック タワー ワイド 》",
                    ["山崎実業", "tower", "マグネットバスルームラック", "ワイド"])
# → True（全語を含む）

match_all_keywords("tower マグネットバスルームラック タワー ラージ",
                    ["山崎実業", "tower", "マグネットバスルームラック", "ワイド"])
# → False（「ワイド」を含まない「ラージ」なので不一致）
```

### 誤判定防止（1語だけの登録を無効化）

`["tower"]`や`["マーナ"]`のような1語だけの登録は、無関係な商品まで
巻き込んでしまうリスクが高い。そのため、`_MIN_MATCH_KEYWORDS = 2`未満の
`match_keywords`は、登録（`append_posted_items`）自体は妨げないが、
索引の構築（`build_posted_index`）時に無視され、一致判定に一切使われない
（安全側に倒す設計）。「型番など十分に一意な識別子なら1語でも可」という
例外は、プログラムで機械的に判定するのが難しい（型番かどうかを自動判定する
基準が無い）ため実装せず、常に2語以上を必須にしている。1語しかない固有の
商品名（例：「ぶんぶんチョッパー」）は、意味のある部分に分割して2語以上に
することで登録している（例：`["ぶんぶん", "チョッパー"]`）。

一般語（収納・掃除・キッチン・便利・ラック・ケース等）だけの組み合わせは、
プログラムでは判定できないため実行時のガードは設けていない。過去投稿を
登録する際に、ブランド名＋商品固有名詞を基本にすることで対応している
（下記「過去投稿の初期登録」参照）。

### 過去投稿の初期登録

`data/past_posted_items_seed.json`に、ブランド名＋商品固有名詞が明確に
確認できた22件を登録した（例：「マーナ シートケース」「tower
マグネットバスルームラック」「ウタマロクリーナー」等）。`item_code`・
商品URLが分からないため、いずれも`match_keywords`（該当する場合は
`product_name`も）で登録している。「tower マグネットバスルームラック」は
スクリーンショットで「ワイド」サイズと確認できたため、`match_keywords`に
`"ワイド"`を含めて「ラージ」等の別サイズと誤判定しないようにしている。

商品の種類・サイズ・個数が曖昧で、推測で登録すべきでないと判断したものは
`data/past_posted_items_unresolved.json`に候補として書き留める運用にした
（今回時点では該当する情報が無かったため空のまま）。本番の
`data/posted_items.json`には、確認が取れたものだけを登録する。

取り込みは既存の`src/import_posted_items.py`（変更なし）をそのまま使う。

```bash
python -m src.import_posted_items data/past_posted_items_seed.json
```

`dedupe.append_posted_items`は、`item_code`・商品URL・商品名・
`match_keywords`のいずれか1つでもあれば登録を受け付ける（今回、
`match_keywords`だけのレコードも受け付けるように条件を追加した）。

### GitHub Actions Summaryへの表示

`storage.build_posted_history_summary_markdown`に、`match_keywords`による
除外件数を追加した。

- item_codeによる除外件数
- URLによる除外件数
- 商品名履歴による除外件数
- match_keywordsによる除外件数

既存の3項目の表示・計算方法は変更していない。

便利グッズ5件＋消耗品/飲料5件・レビュー条件・既存のitem_code/URL/商品名
重複判定・紹介文生成・毎日15:30 JST自動実行・スマホ投稿ページ・投稿済み
データコピー・GitHub Pages・Secretsは変更していない。

## 22. TikTok投稿用コンテンツ生成機能（1商品選定＋台本・テロップ等のテキスト素材生成）

毎日の投稿候補10件（便利グッズ5件＋消耗品/飲料5件）から、TikTokで紹介しやすい
商品を1件だけ自動選定し、台本・テロップ・ナレーション・キャプション・
ハッシュタグ・動画制作メモを自動生成する機能を追加した。今回はテキスト素材の
生成とスマホでの確認までで、動画ファイルの生成・外部の動画生成API
（Runway等）連携・TikTokへの自動投稿は実装していない（意図的に、まずは
無料/低コストで人間が「商品画像＋テロップ＋AI音声＋BGM」形式の動画に
編集しやすい素材を作るところで止めている）。

### 商品選定（`src/tiktok_selector.py`）

room/data/candidates.json（ROOM側の投稿ページ用に書き出し済みの、上位10件の
データ）を入力として受け取り、以下の優先順位で1件だけ選ぶ。

1. 「暮らしの便利グッズ」枠を、「消耗品・飲料」枠より基本的に優先する
   （見た瞬間に用途が分かりやすく、悩み→解決を短時間で伝えやすいため）
2. 掃除・収納・キッチン・時短に関連する商品（カテゴリ名・商品名にこれらの
   言葉を含む）を優先する
3. レビュー件数をタイブレーク程度の軽い加点として使う（上限を設け、
   件数の差だけで優先順位が決まりすぎないようにしている）

「毎回同じジャンルにならないように」という要件のため、`data/tiktok_history.json`
に直近の選定履歴（日付・item_code・カテゴリ・group_label、直近30件まで保持）を
記録し、直近3件と同じカテゴリの商品は減点する（完全に除外はしない。候補の
ジャンルが少ない日でも0件になってしまわないようにするため）。

また、ROOM側の重複除外（`data/posted_items.json`）は「実際にROOMへ投稿済みか」
だけを見るため、まだ人間が投稿していない商品はROOM候補の上位10件に何日も
残り続けることがある。そのままだとTikTok選定はカテゴリだけしか見ていないため、
同じ商品が連日選ばれてしまう可能性があった（tiktok-affiliate-001での監査で
判明）。そのため、`_recent_item_codes()`で履歴ファイルが保持している範囲
（直近`HISTORY_KEEP_LAST`件）のitem_codeを集め、`select_for_tiktok()`で
その商品を候補から基本的に除外するようにした。ただし、本日の候補が全て
過去に選定済みの商品しかない日まで除外すると選定不能（0件）になってしまう
ため、その場合だけ除外せず通常どおりカテゴリベースのスコアリングで選定する
（`not_recently_featured or candidates`というフォールバック）。カテゴリの
ローテーション（減点方式）はそのまま維持しており、今回追加したitem_code単位の
除外は、それとは別の「毎回同じ商品にならないように」という観点の重複防止。

### コンテンツ生成（`src/tiktok_content_generator.py`）

実際に商品を購入・使用していない前提のため、「買ってよかった」「使ってみた」
「愛用しています」等の体験談を装う表現は一切生成しない。これを確実にするため、
新しく文章テンプレートを作るのではなく、紹介文生成（`description_generator.py`）
がすでに使っている、商品情報だけを根拠にした安全な言い回しのテンプレート
（`PRODUCT_TYPE_TEMPLATES`・`GENERIC_TEMPLATES`）を再利用する構成にした。

`description_generator.py`に`get_template_components()`と
`match_product_type_keyword()`を追加した（既存の`generate_description()`や
テンプレートの中身は一切変更していない。新しい関数は、同じテンプレートから
「悩み・解決・メリット・締め」の内容をそのまま取り出すだけの、既存関数とは
独立した読み取り専用のヘルパー）。これにより、TikTok台本用に別のテンプレート
一式を新しく書く必要がなく、紹介文生成の安全な言い回しルール（体験談を装わない・
確認できない性能を推測しない等）がそのままTikTok台本にも適用される。

台本は、0〜3秒（フック）・3〜8秒（悩み・共感）・8〜14秒（特徴2項目）・
14〜18秒（まとめ）・18〜20秒（楽天ROOMへの導線）という構成にしている。
ナレーションはAI音声でそのまま読み上げられるよう、装飾記号（絵文字等）を
取り除いて句点で終わる文に整えている（`estimate_duration_seconds()`で
文字数から読み上げ秒数の目安を見積もり、テストで15〜20秒程度に収まって
いることを確認している。厳密なTTS計測ではなく簡易的な目安）。

ハッシュタグは、「#買ってよかった」「#購入品」のような、実際に購入した
という誤解を招くタグを含めない（ユーザーからの指示例に`#楽天購入品`が
含まれていたが、同じ指示の中の「購入したという誤解を招くタグは避ける」
という明確なルールと矛盾していたため、禁止ルールの方を優先し、
`#楽天購入品`は採用しなかった）。

### データの流れとGitHub Actionsへの組み込み

`src/tiktok_daily.py`が、`room/data/candidates.json`を読み込み→
`tiktok_selector.select_for_tiktok()`→`tiktok_content_generator.build_content()`
→`tiktok/daily_content.json`・`tiktok/daily_content.md`の書き出し→
`tiktok_selector.record_selection()`（選定履歴の記録）という一連の処理を行う。
`room/data/candidates.json`が存在しない・候補が0件の場合はSystemExitし、
不完全なデータを書き出さない。

`.github/workflows/search_candidates.yml`に、ROOM候補生成
（`python -m src.main`）→スマホ投稿ページのデータ書き出し
（`python -m src.publish_room_page`）の**直後**のステップとして
`python -m src.tiktok_daily`を追加した。GitHub Actionsは前のステップが
失敗すると後続ステップを実行しないデフォルト挙動があるため、ROOM候補生成が
失敗した回は、このステップも「投稿ページ・TikTok用データをコミットして
プッシュ」ステップも実行されず、TikTok側だけ単独で不完全なデータを
作ってしまうことはない。既存のROOM候補生成・重複チェック・紹介文生成・
5+5選定ロジック自体には一切手を加えていない。

毎日15:30の自動実行（`run_scheduled_search.yml`）は、`search_candidates.yml`を
起動するだけの仕組みのため、この変更だけで自動実行・手動実行
（workflow_dispatch）の両方に対応できている。

### スマホ表示（`tiktok/index.html`）

room/index.htmlと同様のモバイル最適化した単一の静的ページ。商品画像・
商品名・選定理由・台本・テロップ・ナレーション・キャプション・ハッシュタグ・
動画制作メモを表示し、それぞれに「コピー」ボタンを付けている。スマホ幅
390pxでの横スクロールが出ないこと、コピー機能が実際に動作することを、
Playwrightを使った実機での確認テストで検証済み。

便利グッズ5件＋消耗品/飲料5件・レビュー条件・投稿済み履歴・4段階の重複判定・
紹介文生成・毎日15:30 JST自動実行・スマホ投稿ページ（room/）・GitHub Pages・
Secretsは変更していない。

## 23. TikTok日次生成の本番運用向け堅牢化（tiktok-affiliate-002）

TikTokアフィリエイト機能を本番の日次運用で安定して使える状態にするため、
監査を行い、見つかった不足を修正した。

### 発見した問題1: TikTok側だけ失敗すると、成功していたROOM側の更新まで失われる

`search_candidates.yml`は、ROOM候補生成（`src.main`）→スマホ投稿ページの
データ書き出し（`src.publish_room_page`）→TikTokコンテンツ生成
（`src.tiktok_daily`）→コミット・pushという順番のステップで構成されている。
GitHub Actionsは前のステップが失敗すると後続ステップを実行しない
デフォルト挙動があるため、**ROOM候補生成・投稿ページのデータ書き出しは
成功していても、TikTokコンテンツ生成だけが失敗した場合、最後の
「コミットしてプッシュ」ステップごと実行されず、せっかく成功していた
ROOM側の新しい候補データ（`room/data/candidates.json`）までコミットされずに
失われてしまう**問題があった（22章で書いた「ROOM側が失敗した回はTikTok側の
不完全なデータを作らない」という設計は正しいが、その逆方向＝TikTok側だけが
失敗した場合の対策が抜けていた）。

対応として、最後の「投稿ページ・TikTok用データをコミットしてプッシュ」
ステップに`if: always()`を付け、TikTokコンテンツ生成ステップ
（`id: tiktok`）が失敗・スキップされた場合でも実行されるようにした。
ただし、`steps.tiktok.outcome == 'success'`のときだけ`tiktok/`・
`data/tiktok_history.json`を`git add`の対象に含めるようにし、TikTok側が
失敗した場合はROOM側のデータ（`room/data/candidates.json`）だけを
コミットするようにした。これにより、

- ROOM候補生成自体が失敗した場合：`publish_room_page`・`tiktok_daily`の
  両方が実行されないため、どちらのファイルも変化せず、結果的に何も
  コミットされない（従来どおり安全）。
- ROOM側は成功・TikTok側だけ失敗した場合：ROOM側のデータだけが
  コミットされ、TikTok側（`tiktok/`・`data/tiktok_history.json`）は
  前回の内容のまま維持される（新しく追加した挙動）。
- 両方成功した場合：従来どおり両方コミットされる。

という3パターンいずれでも、コミットされる内容が常に「そのステップまでの
処理が成功して書き出されたファイルだけ」になり、`tiktok/daily_content.json`
（新しい選定結果）と`data/tiktok_history.json`（選定履歴）が矛盾した
組み合わせでコミットされることもない（TikTok側のファイルは、生成処理
（`src.tiktok_daily`の`main()`）が最後まで成功したときだけまとめて
コミット対象になるため）。

また、TikTok生成ステップが失敗した場合に、実行ログを開かなくても
Actionsの実行結果ページ（Summary）から状況が分かるよう、
「TikTok生成の失敗をActions Summaryに分かりやすく記録する」ステップを
追加した（`if: failure() && steps.tiktok.outcome == 'failure'`）。

### 発見した問題2: 同じ日に手動で複数回実行すると、履歴に同日の記録が何件も積み重なる

`tiktok_selector.record_selection()`は、これまで選定結果を無条件で
履歴に追記するだけだった。同じ日にGitHub Actionsを手動で複数回実行すると
（例えば1回目の実行結果を確認してから、設定を調整してもう一度実行する等）、
`data/tiktok_history.json`に同じ日付の記録が複数件積み重なってしまう
（履歴が壊れるわけではないが、直近の選定履歴が本来より多くの「実際に
選ばれた日」を含んでいるかのように見えてしまう、不自然な状態）。

対応として、`record_selection()`で新しい記録を追記する前に、同じ日付
（`entry["date"]`）の既存の記録を取り除くようにした。これにより、同じ日に
何度実行しても、履歴に残るのはその日最後に実行した選定結果1件だけになる。

### 発見した問題3: 書き込み途中でプロセスが終了すると、JSONファイルが壊れた状態で残る可能性

`data/tiktok_history.json`・`tiktok/daily_content.json`・
`tiktok/daily_content.md`は、いずれも`open(path, "w")`で直接書き込んで
いた。GitHub Actionsのジョブタイムアウトや手動キャンセル等でこの書き込みの
途中にプロセスが終了すると、書きかけの不完全なファイル（壊れたJSON等）が
残ってしまう可能性があった。

対応として、`src/atomic_io.py`を新規追加し、「同じディレクトリに一時
ファイルを書き、書き終わってから`os.replace()`で本来のファイル名に
置き換える」というアトミックな書き込み方式に変更した（`os.replace()`は
同一ファイルシステム上ではアトミックな操作であるため、置き換えの途中で
壊れた中間状態のファイルが観測されることはない）。`tiktok_selector.py`の
履歴保存と、`tiktok_daily.py`のJSON/Markdown書き出しの両方で使っている。

### 確認して問題が無かった項目

- **日次実行をまたいだ履歴の永続化**: `data/tiktok_history.json`は
  毎回のワークフロー実行の最後にコミット・pushされ、次回の実行は
  `actions/checkout@v4`でその最新状態から始まるため、日をまたいだ
  永続化の仕組み自体は元から正しく機能していた（今回の問題1は、
  「失敗時にコミットされない」という別の問題）。
- **GitHub Actionsが生成物だけを安全にコミットすること**: 最後のコミット
  ステップは`git add -A`等ではなく、`room/data/candidates.json`・
  `tiktok/daily_content.json`・`tiktok/daily_content.md`・
  `data/tiktok_history.json`の4ファイルだけを明示的に指定しているため、
  `.env`や`data/candidates/`（`.gitignore`対象）等の意図しないファイルが
  混入する余地はない。変更不要と判断した。
- **iPhone向け`tiktok/index.html`のデータ連携**: `daily_content.json`を
  キャッシュを無視して取得し（`cache: "no-store"`・タイムスタンプ付き
  クエリ）、取得失敗時・商品名が無い場合にそれぞれ分かりやすいメッセージを
  表示する作りになっており、商品名・台本・テロップ・ナレーション・
  キャプション・ハッシュタグ・動画制作メモそれぞれに「コピー」ボタンが
  付いている。追加の問題は見つからなかった。

### テスト

- `tests/test_tiktok_selector.py`に、同じ日付の記録が複数回追記されても
  履歴には1件しか残らないことを確認するテストを追加した。
- `tests/test_atomic_io.py`を新規追加し、`atomic_io.write_text_atomic()`・
  `write_json_atomic()`が正しく書き込めること、書き込み中に例外が起きても
  既存のファイルが壊れず・一時ファイルも残らないことを確認した。

## 24. 投稿済み登録の手動作業を減らす（GitHub Actionsからの登録）

「ROOMで実際に投稿した商品を、毎回手作業でChatGPTへJSONを貼ったり、手動で
`import_posted_items`を実行したりしなくても、`data/posted_items.json`へ
安全に反映できるようにしたい」という要望への対応。

### 調査結果：「ROOMへ本当に投稿されたこと」の完全自動判定は不可能

楽天ROOMは、投稿履歴を読み取れる公式APIを提供していない（このプロジェクトが
使っている楽天ウェブサービスのAPIは商品検索専用で、ROOM投稿とは無関係）。
ROOMの投稿状況をプログラムから確認する唯一の方法は、ROOMへの自動ログイン・
Cookie/セッションの利用・画面のスクレイピングだが、これはこのプロジェクトの
安全ルール（CLAUDE.md・各AI_TASKの安全ルール）で明確に禁止されている。

したがって、「候補に出ただけの商品」と「実際にROOMへ投稿し終えた商品」を
プログラムだけで区別する手段は無く、**最終的には人間の自己申告（スマホ投稿
ページの「投稿済みにする」ボタンを押す、という操作）に頼るしかない**。これは
今回新しく分かった制約ではなく、従来の「投稿済みデータをコピー→
`import_posted_items`で取り込む」という仕組みも、そもそも同じ自己申告を
前提にしていた（今回はその前提を変えていない。「候補に出ただけで自動的に
投稿済み扱いになる」ような実装は、ROOMの利用規約上の理由でも、誤って
未投稿の商品を除外してしまう事故を防ぐ意味でも行っていない）。

### 実装した範囲：自己申告データの「取り込み経路」の自動化

上記の制約を踏まえ、「本当に投稿されたことの自動判定」ではなく、**既存の
自己申告（スマホ投稿ページでの「投稿済みにする」）を`posted_items.json`へ
反映するまでの手作業（JSONをファイルに保存する・ローカルでPythonコマンドを
実行する・git commit/pushする）を無くす**ことを対象にした。

- `.github/workflows/import_posted_items.yml`（新規）：`workflow_dispatch`
  （GitHubの「Actions」タブから手動実行）で、貼り付けられたJSONを
  `python -m src.import_posted_items`（変更なし・既存の重複判定ロジックを
  そのまま利用）に渡し、`data/posted_items.json`が変化した場合だけ
  コミット・pushする。
  - 実行できるのはこのリポジトリへの**書き込み権限を持つ人だけ**
    （`workflow_dispatch`はGitHub標準の権限モデルで自動的にそうなるため、
    このワークフロー側で追加の権限チェックは実装していない。GitHub
    Issueなど、第三者が起票できる仕組みをトリガーにする案も検討したが、
    権限チェックを自前で実装する必要があり、実装ミスがあれば第三者が
    任意のデータを`posted_items.json`へ混入できてしまうリスクがあるため
    見送った）。
  - 入力値（`${{ inputs.posted_items_json }}`）は`run:`スクリプトの文字列に
    直接埋め込まず、`env:`経由で渡してから`printf '%s' "$VAR" > ...`で
    ファイルに書き出している（GitHub公式が推奨する、シェルインジェクションを
    避ける受け渡し方）。
  - JSONの妥当性チェック・重複判定は新しいロジックをここに書かず、既存の
    `src.import_posted_items`→`src.dedupe.append_posted_items`にすべて
    任せている（判定ロジックの二重実装を避けるため）。
- `src/import_posted_items.py`：`json.load()`が失敗した場合に生の
  トレースバックではなく、分かりやすい日本語メッセージで`SystemExit`する
  ように改善した（既存のCLI利用・ワークフロー経由の利用どちらにも効く）。
  それ以外の重複判定・取り込みロジックは変更していない。
- `room/index.html`：「① 投稿済みデータをコピー」の隣に「② GitHubへ登録する」
  リンク（Actionsのワークフロー実行ページを新しいタブで開く）を追加した。
  コピーするJSONは、GitHub Actionsの入力欄（改行を保持しない環境がある）
  に安全に貼り付けられるよう、整形（インデント付き）ではなく1行のJSONに
  変更した（`JSON.stringify(records, null, 2)` → `JSON.stringify(records)`。
  `import_posted_items`側はJSONの改行の有無を区別しないため、既存の
  ローカルファイル経由の取り込みには影響しない）。

### 自動化後の流れ

1. スマホ投稿ページでROOMへ実際に投稿した商品を「投稿済みにする」で記録する
   （ここは変更なし。人間の自己申告）。
2. 「① 投稿済みデータをコピー」でJSONをコピーする（変更なし）。
3. 「② GitHubへ登録する」を開き、GitHubの「Actions」タブで「投稿済み商品を
   登録する」ワークフローの「Run workflow」→入力欄にJSONを貼り付け→実行する
   （新規。ここまでパソコン・Python・git操作・ChatGPTへの貼り付けは不要）。
4. ワークフローが`data/posted_items.json`を更新し、自動でコミット・push
   する（新規）。

オーナー側に残る操作は「実際にROOMへ投稿する」「スマホ投稿ページで
投稿済みにする」「コピーしたJSONをActionsの入力欄に貼り付けてRun workflowを
押す」の3点で、いずれも人間の確認・操作が必要な部分（ROOMへの投稿そのもの、
投稿完了の自己申告）であり、安全ルール上も自動化できない・すべきでない部分。

### テスト

- `tests/test_import_posted_items.py`に、壊れたJSON・空ファイルを渡した
  場合に分かりやすいメッセージで`SystemExit`することを確認するテストを
  追加した。
- `.github/workflows/import_posted_items.yml`のYAML構文を`yaml.safe_load`
  で確認した。
- `room/index.html`の変更後もHTMLとして正しくパースできることを
  `html.parser`で確認した。

## 25. 紹介文生成の用途誤り・カテゴリー内の同一文章化を修正

本番運用で、次の2つの問題が報告された。

1. 洗濯洗剤の商品なのに「普段のお洗濯や食器洗いに使いやすい」という、
   確認できない食器洗い用途が紹介文に混ざっていた。
2. 「洗剤」「水」「お茶」「ジュース」等のカテゴリーで、商品が変わっても
   毎日ほぼ同じ紹介文（書き出し・悩み・解決部分）が生成されていた。

### 原因調査

`src/description_generator.py`の`GENERIC_TEMPLATES`（商品タイプが
`PRODUCT_TYPE_TEMPLATES`で特定できない場合に使う、カテゴリー共通のテンプレート）
を確認したところ、次の2点が根本原因だった。

- **`GENERIC_TEMPLATES["洗剤"]`のcheckoutlist_coreが、用途を商品名から
  確認せずに常に「普段のお洗濯や食器洗いに使いやすい」と断定していた。**
  `PRODUCT_TYPE_TEMPLATES`（商品の種類がほぼ確実に分かる場合の専用
  テンプレート）には、リモコン・水切りボウル・スープメーカーのような
  「暮らしの便利グッズ」枠の商品タイプは16件登録されていたが、洗剤・水・
  お茶等の「消耗品・飲料」枠の商品タイプは1件も登録されておらず、
  必ずカテゴリー共通のGENERIC_TEMPLATESにフォールバックしていた。その
  GENERIC_TEMPLATES側が、洗濯専用・食器専用・住宅用（掃除用）のどの
  洗剤にも同じ「お洗濯や食器洗い」という2つの用途を毎回断定していたため、
  洗濯専用の商品でも食器洗い用途が誤って書かれていた。
- **GENERIC_TEMPLATESは1カテゴリーにつき1つの固定テンプレート（hook_text・
  worry_lines・solution_textはそれぞれ単一の文字列）で、closing_variants
  （締めの一言）だけが2パターン用意されていた。** さらに、✔️メリットの
  3項目目に使う`FEATURE_CLAUSES`（商品名から読み取れる構造・仕様の一覧）には、
  マグネット・折りたたみ等の「暮らしの便利グッズ」向けの特徴語しか登録
  されておらず、洗剤・飲料に特有の特徴語（詰め替え・無香料・無糖・
  ノンカフェイン・紙パック等）が1件も登録されていなかった。そのため、
  同じカテゴリー内のほとんどの商品で3項目目もchecklist_fallback
  （商品タイプ共通の同じ言い回し）になり、実質的に「毎日ほぼ同じ文章」に
  なっていた。

いずれも「新しい固定テンプレートを大量に増やす」以前に、**(a)洗剤の用途を
商品名から確認する仕組みが無かったこと、(b)消耗品・飲料の商品固有の特徴を
拾う仕組み（FEATURE_CLAUSES）が無かったこと、(c)文言のパターンが
closing_variants以外に無かったこと**が根本原因だった。

### 修正内容（原因に対応した最小限の変更）

- **`DETERGENT_SUBTYPE_TEMPLATES`（新規）**：商品名から「洗濯用」
  （洗濯・衣類用・部屋干し・柔軟剤）「食器用」（食器・台所）「住宅用＝
  掃除用」（住宅用・浴室用・トイレ用・換気扇用）のいずれかが確認できる
  場合、その用途専用のテンプレートを使う（`PRODUCT_TYPE_TEMPLATES`と
  同じ、キーワード一致で最初に一致したものを採用する方式）。用途が
  確認できない場合（例：「濃縮洗剤」のように用途を明記していない商品）は、
  引き続き`GENERIC_TEMPLATES["洗剤"]`を使うが、そのchecklist_coreを
  「普段のお手入れに使いやすい」という、特定の用途を断定しない安全な
  言い回しに修正した（「お洗濯や食器洗い」の断定を削除）。
- **`_PostTemplate`に`hook_variants`・`worry_variants`・
  `solution_variants`（複数パターン）を追加**（デフォルトは空タプルで
  未使用＝既存の16件のPRODUCT_TYPE_TEMPLATESは一切変更していない）。
  洗剤（用途別サブタイプ含む）・キッチン消耗品・日用品・水・お茶・
  ジュースの各テンプレートに、①キャッチコピー②悩み③解決文をそれぞれ
  2パターンずつ追加し、商品コードに応じて異なる組み合わせが選ばれる
  ようにした（`seed % 2`・`(seed // 7) % 2`・`(seed // 13) % 2`と、
  法を変えて3つの要素が同じタイミングで一緒に変わらないようにしている）。
  closing_variantsも2件→4件に増やした。
- **`FEATURE_CLAUSES`に消耗品・飲料向けの特徴語を追加**：詰め替え／
  つめかえ、無香料／無香性、業務用（洗剤）、無糖／砂糖不使用、
  食塩不使用／無塩、ノンカフェイン／カフェインレス／カフェインゼロ、
  紙パック、水出し（飲料）。いずれも商品名に明記されている場合だけ
  ✔️メリットの3項目目に反映され、確認できない場合は
  checklist_fallbackのまま（新しい特徴を推測で追加することはない）。
- **`generate_description()`・`get_template_components()`の重複していた
  テンプレート選択ロジックを`_select_template()`・
  `_pick_hook_worry_solution()`に共通化した。** これにより、TikTok台本
  生成（`get_template_components()`経由）にも同じ用途判定・安全な
  フォールバックが自動的に適用される（TikTok側で洗濯洗剤の台本に
  「食器洗い」が混ざる、という同種の問題も同時に修正されている）。

### 意図的に対応を見送った点（過剰な変更を避けるため）

- 紹介文の6ブロック（①キャッチコピー②悩み③解決④✔️メリット⑤締め
  ⑥ハッシュタグ）の**並び順そのもの**は変更していない。要望では
  「悩みから始める／特徴から始める」等の構成変化も挙げられていたが、
  `tests/test_description_generator.py`の複数のテスト（`blocks[0]`が
  キャッチコピー、`blocks[1]`が悩み、`blocks[2]`が解決文…という前提）が
  ①→⑥の並び順を前提にしており、ROOM投稿としての読みやすい構成
  （絵文字始まり→共感→解決→メリット→締め→タグ）自体は安全ルール■7
  （現在のROOM投稿フォーマットを維持）にも合致するため、**内容
  （文言）のバリエーションで「毎回同じ構成に見えない」ことを実現する**
  方針にした。
- ✔️メリットの箇条書きの数（3個固定）も変更していない。要望では
  「3個固定にする必要はない」と許可されていたが、必須の変更ではなく、
  件数を可変にすると既存のテスト（`test_checklist_has_exactly_three_
  checked_items`）や表示形式への影響が増えるわりに得られる変化が
  小さいと判断し、今回は見送った。
- 直近に生成した紹介文との類似度を確認し、似ていれば再生成する仕組み
  （要望■5）は実装していない。実現するには、TikTok側の
  `data/tiktok_history.json`のような「紹介文生成の履歴ファイル」を
  新設する必要があり、影響範囲・複雑さが大きいため、今回は
  「そもそも同じ組み合わせが生成されにくくなるよう文言パターンを
  増やす」ことで再発を防ぐ方針にした（要望にも「可能であれば」と
  ある、必須ではない項目）。将来的に必要であれば、TikTokの選定履歴と
  同様の仕組みを追加することは可能。

### テスト

- `tests/test_description_generator.py`に`DetergentSubtypeTest`
  （洗濯用・食器用・住宅用洗剤で確認できない用途を書かないこと、
  用途が確認できない場合は断定しないこと）・`ConsumableVarietyTest`
  （異なる飲料・洗剤商品で紹介文が完全一致しないこと、商品固有の特徴
  （無糖・ノンカフェイン・詰め替え等）が反映されること、確認できない
  特徴を勝手に追加しないこと）を追加した。
- 既存の`test_detergent_hook_matches_requested_wording`・
  `test_water_hook_matches_requested_wording`・
  `test_tea_hook_matches_requested_wording`は、hook_variants追加に伴い
  「複数用意された安全な文言のいずれかである」ことを確認する形に更新した
  （元のテストは単一の固定文言との完全一致を確認していたが、今回の
  変更で意図的に複数パターンになったため）。
- 既存の全テスト（商品候補生成・重複判定・GitHub Actions関連の設定・
  TikTok生成等、今回変更していない機能のテスト）が引き続き成功することを
  確認した。

## 26. 投稿済み履歴（`data/posted_items.json`）の書き込みをアトミック化（reliability-003）

「23. TikTok日次生成の本番運用向け堅牢化」（`src/atomic_io.py`新設）では、
`data/tiktok_history.json`・`tiktok/daily_content.json`・
`tiktok/daily_content.md`の書き込みをアトミック化したが、ROOM側の
投稿済み履歴（`data/posted_items.json`）を書き換える唯一の関数
`dedupe._save_posted_items()`は対象外のままで、引き続き
`path.open("w")`＋`json.dump()`で直接書き込んでいた。GitHub Actionsの
ジョブタイムアウトや手動キャンセル等でこの書き込みの途中にプロセスが
終了すると、日次実行のたびに読み書きする本番の重複チェック履歴が
壊れた状態で残ってしまう可能性があった。

### 対応

`dedupe._save_posted_items()`を、新規実装を追加せず既存の
`atomic_io.write_json_atomic()`を再利用する形に変更した（同じ書き込み
処理を重複実装しないため）。読み込み側（`load_posted_items`等）・
重複判定ロジック（`match_posted_reason`等）・`append_posted_items()`の
シグネチャや戻り値は変更していない。

### テスト

- `tests/test_dedupe.py`に`AppendPostedItemsAtomicWriteTest`を追加し、
  次を確認した。
  - 書き込み途中（`os.fdopen`）で例外が起きても、既存の
    `posted_items.json`の内容が変更前のまま残ること
  - 一時ファイルが残らないこと
  - `_save_posted_items()`が実際に`atomic_io.write_json_atomic()`を
    経由していること（実装の再利用を回帰させないため）
- 既存の`AppendPostedItemsTest`（正常な保存・再読込、item_code／URL／
  商品名／match_keywordsによる重複判定の既存挙動）を含め、全217件の
  自動テストが引き続き成功することを確認した。

## 27. 候補一覧・投稿ページ用データの書き込みをアトミック化（reliability-004）

「26. 投稿済み履歴の書き込みをアトミック化（reliability-003）」の
「次にChatGPTが判断すべき点」で保留となっていた、`src/storage.py`の
`save_candidates()`（`data/candidates/candidates_*.json`・`.md`）と
`src/publish_room_page.py`の`main()`（`room/data/candidates.json`）の
2箇所を監査した。いずれも`path.open("w")`＋`json.dump()`／
`Path.write_text()`で直接書き込んでおり、書き込み途中にプロセスが
終了すると壊れたファイルが残る可能性があった。

- `data/candidates/candidates_*.json`は`publish_room_page.py`の
  `find_latest_candidates_json()`が「最も新しいファイル」としてそのまま
  読み込むため、壊れかけのファイルが残ると次のステップ（投稿ページ用
  データ生成）が壊れたJSONを読み込んで失敗する可能性があった。
- `room/data/candidates.json`は投稿ページ（`room/index.html`）が
  ブラウザから直接読み込むファイルであり、壊れると投稿ページの表示に
  影響する。

### 対応

いずれも新規実装を追加せず、既存の`atomic_io.write_json_atomic()`・
`atomic_io.write_text_atomic()`を再利用する形に変更した。

- `src/storage.py`の`save_candidates()`: JSON・Markdownの両方を
  `atomic_io`経由の書き込みに変更した。戻り値（保存先パスのタプル）・
  Markdown整形（`render_candidates_markdown()`等）は変更していない。
- `src/publish_room_page.py`の`main()`: `room/data/candidates.json`の
  書き込みを`atomic_io.write_json_atomic()`経由に変更した。
  `build_room_page_data()`によるデータ整形・`find_latest_candidates_json()`
  による最新ファイル検索のロジックは変更していない。

商品検索条件・重複判定・紹介文生成・TikTok関連（`src/tiktok_*.py`）・
`data/posted_items.json`まわり（前章で対応済み）には一切手を加えていない。

### テスト

- `tests/test_storage.py`を新規追加し、次を確認した。
  - 正常にJSON・Markdownが保存され、戻り値のパスが存在すること
  - 出力先ディレクトリが無い場合でも自動作成されること
  - 書き込み途中（`os.fdopen`）で例外が起きても、出力先ディレクトリに
    壊れかけのファイルが残らないこと
  - JSON・Markdownの保存が実際に`atomic_io`経由であること
- `tests/test_publish_room_page.py`に`MainAtomicWriteTest`を追加し、
  次を確認した。
  - `main()`が正常に`room/data/candidates.json`を書き出すこと
  - 書き込み途中で例外が起きても、既存の`room/data/candidates.json`が
    変更前のまま残り、一時ファイルも残らないこと
  - `main()`が実際に`atomic_io.write_json_atomic()`経由で書き込んでいること
- 既存の全自動テスト（`python-dotenv`未インストールによる
  `tests.test_main`の収集エラーを除く）が引き続き成功することを確認した
  （このサンドボックス環境固有の制約で今回の変更とは無関係。詳細は
  `docs/AI_STATUS.md`参照）。

## 28. 紹介文生成の「カテゴリー→テンプレート」依存を減らし、商品固有情報を優先する（第2段階）

25章の修正（洗濯洗剤に食器洗いが混ざる問題）のあと、2026-09-19生成分の
`room/data/candidates.json`を実際に確認したところ、次の問題がまだ残っていた。

1. 収納3商品（ハンガー・段ボールストッカー・ヘアアイロンポーチ）が、
   いずれも`GENERIC_TEMPLATES`のカテゴリー共通文言（「身の回りの物の
   置き場所、決まってる？」等）に頼り、商品固有の特徴がほぼ反映されて
   いなかった。
2. バターカッターが「毎日の料理や後片付け」という一般的な`GENERIC_TEMPLATES
   ["キッチン"]`の文言になり、ワイヤーで切る・計量する等の商品固有情報が
   使われていなかった。
3. 洗剤・水・お茶・ジュース等が「ストック」を主題にした文言に偏っており、
   商品ごとの角度の違いがほとんど無かった。
4. 衣類しみ抜き剤が`GENERIC_TEMPLATES["洗剤"]`にフォールバックし、
   「毎日のように使う洗剤だから」という、しみ抜き剤には当てはまらない
   使用頻度の断定が書かれていた。
5. 「お1人様ご家族様1本限り」と明記されたグレープジュースの紹介文に
   「まとめてストックしておけそう」「まとめ買いしておきたい人に便利そう」
   という、購入制限と明確に矛盾する文章が生成されていた。

### 対応方針

要望どおり、「カテゴリー→テンプレート→商品名を少し差し込む」という
順序から、「商品固有情報を抽出→商品タイプ・特徴を判断→紹介するポイントを
選択→文章を組み立てる」という順序へ寄せる方向で、原因ごとに対応した。
固定テンプレートを無闇に増やすのではなく、(a)今回明確にテンプレートが
不足していた4商品タイプだけをPRODUCT_TYPE_TEMPLATESに追加し、(b)それ以外は
「商品名から確認できる事実を抽出する仕組み」を新設して、個別のテンプレート
追加に頼らず今後の商品にも効くようにしている。

- **`PRODUCT_TYPE_TEMPLATES`に4件追加**（ハンガー・段ボールストッカー／
  ダンボールストッカー・ヘアアイロンポーチ・バターカッター）。いずれも
  ユーザーが商品名から確認した特徴（滑り止め・段ボール専用・耐熱・
  ワイヤーで切る等）だけを使っている。
- **`DETERGENT_SUBTYPE_TEMPLATES`に「しみ抜き」サブタイプを追加**
  （`_STAIN_REMOVER_TEMPLATE`）。「洗濯」等のキーワードより先に判定する
  ことで、しみ抜き剤が洗濯用洗剤テンプレートへ誤ってフォールバックしない
  ようにし、「毎日のように使う」という頻度の断定をしない専用の文言にした。
- **`_extract_quantity_phrase()`（新規）**：商品名から容量・個数・
  カット量等の「確認できる数字付きの事実」（例：「100本セット」
  「5gカット」「200ml」）を正規表現で抜き出し、他の✔️メリット項目と
  重複しない場合に限り4項目目として追加する（商品名にそのまま書かれている
  表記だけを使い、推測・言い換えはしない。何も見つからなければ従来どおり
  3項目のまま＝箇条書きの数を必ず3個に固定しない、という運用方針の反映）。
  「1本限り」のような購入制限の表記は内容量ではないため、直後に
  「限り／まで／限定」が続く数字付き表記は除外している。
- **`_is_purchase_limited()`・`_filter_bulk_buying()`（新規）**：商品名に
  購入制限を示す表現（「お一人様」「◯本限り」「数量限定」等、正規表現で
  一般化した判定。今回の商品名だけのハードコードではない）が確認できる
  場合、hook_variants・worry_variants・solution_variants・closing_variants
  の候補から、まとめ買いを連想させる言い回し（「まとめ買い」「まとめて」
  「複数」「大量」）を取り除いてから選ぶようにした。取り除いた結果0件に
  なる場合は安全側に倒して元の候補をそのまま使う（選択不能にはしない）。
- **消耗品・飲料の`GENERIC_TEMPLATES`（洗剤フォールバック・キッチン消耗品・
  日用品・水・お茶・ジュース）に3つ目のhook/worry/solutionパターンを追加**。
  「ストック」を主題にしない角度（水分補給のタイミング・食事のお供・
  気分転換のひととき等）を増やし、ストックが「選べる紹介角度の1つ」に
  なるようにした（要望■3）。
- **`generate_descriptions_for_batch()`（新規、`src/main.py`のフェーズ4で
  使用）**：1回の実行で選ばれた最終候補（最大10件）についてまとめて
  紹介文を生成し、①キャッチコピー・⑤締めの一言が両方一致する（構成が
  強く似た文章になる）組み合わせが出た場合だけ、item_codeに目印を付けて
  別パターンを選び直す（最大3回まで再試行。無限ループにはしない）。
  「直近投稿履歴との類似度チェック」という新しい履歴ファイルを持つ仕組み
  （影響範囲が大きい）は実装せず、「少なくとも同一生成バッチ内で強い
  類似を避ける」という要望の最低ラインを、既存の`generate_description()`
  をそのまま使う軽量な後段処理で満たしている。2026-09-19の実データで、
  からだにユーグレナ（いちごオレ）とドールグレープが個別生成では
  同じ①⑤の組み合わせになる実際の衝突を検出・解消できることを確認した
  （`tests/test_description_generator.py`の`BatchDiversityTest`）。
  `src/main.py`は、紹介文生成のタイミングをフェーズ3（`unique_items`全件）
  からフェーズ4（`select_balanced_top`で選ばれた最終候補のみ）に移動した。
  `ranking.py`の選定処理は`description`フィールドを参照しないため、順序を
  入れ替えても既存の選定結果には影響しない（無駄な生成も減る）。

### 意図的に対応を見送った点

- 「時短・収納・省スペース…」といった紹介角度を包括的に選択する汎用的な
  分類エンジンは実装していない。商品名からの単純な正規表現・キーワード
  抽出の範囲にとどめ、確認できない推測（性能・効果・健康効果等）を
  絶対に作らないという安全ルールを優先した。
- 直近の投稿履歴（前日・前週等）との類似度チェックは実装していない
  （要望内で「無理に追加しなくて構わない」とされていた項目。新しい履歴
  ファイルの導入は影響範囲が大きいため見送った）。同一バッチ内の
  重複回避（`generate_descriptions_for_batch`）で最低ラインの対応をした。

### テスト

`tests/test_description_generator.py`に次を追加した。

- `Sept19BatchRegressionTest`：2026-09-19の実際の商品名を使い、
  ハンガー・段ボールストッカー・ヘアアイロンポーチ・バターカッターへの
  商品固有特徴の反映、しみ抜き剤の頻度断定なし、購入制限商品での
  まとめ買い表現なし（一般化された判定であることの確認含む）、洗濯用
  洗剤の食器洗い再発なし、確認できない特徴を追加しないことを確認。
- `BatchDiversityTest`：`generate_descriptions_for_batch()`が実際の
  データ衝突を解消すること、衝突が無い場合は個別生成と同じ結果になる
  こと、件数が保たれることを確認。
- `CategoryDiversityRegressionTest`：同カテゴリーの複数商品で①⑤の
  組み合わせ・✔️メリットが完全一致しないことを確認。

既存の全テスト（商品検索条件・重複判定・GitHub Actionsの既存ワークフロー・
TikTok関連等、今回変更していない機能のテスト）が引き続き成功することを
確認した。

## 29. 候補不足（7件）の根本原因調査と追加探索・診断情報の追加（description-match-002）

2026-09-21 15:48生成分の`room/data/candidates.json`が
`convenience_count: 3`・`consumable_count: 4`（合計7件、目標は5+5=10件）
だった問題と、この日の紹介文で見つかった4件の誤り（下記31章参照）を
併せて調査・修正した。

### 根本原因（候補不足）

`src/main.py`のフェーズ1（キーワードごとの検索・条件フィルタ・重複除外）を
確認したところ、次の構造的な問題が見つかった。

- **「暮らしの便利グッズ」枠は、5つのカテゴリー（掃除／収納／キッチン／
  時短／暮らし全般）それぞれに検索ワードが1つずつしか無く、代替手段が
  無かった。** そのキーワードの検索結果が、条件フィルタ（レビュー4.0
  以上・100件以上等）・投稿済み履歴による除外・同一実行内の重複除外を
  経て0件になった場合、そのカテゴリーはその日ずっと0件のままになる
  （リトライや別の検索ワードを試す仕組みが無かった）。
- **`data/posted_items.json`（投稿済み履歴）が98件まで増えており**
  （前回セッション終了時点）、検索結果を`reviewCount`降順で取得する
  `rakuten_api.search_items`の性質上、同じキーワードでは毎回同じ
  上位商品（≒売れ筋の定番商品）が返ってきやすく、それらが投稿済みに
  なるにつれて、特にキーワードが1つしか無いカテゴリーで「条件を満たす
  未投稿の商品が無い日」が発生しやすくなっていた。
- `ranking.select_balanced_top()`の「片方の枠が不足したら、もう片方の
  枠の残りから補充する」というクロスグループの補充自体は正しく機能して
  いた（既存テスト`test_convenience_shortfall_is_filled_from_consumable`
  等で確認済み）。今回は便利グッズ・消耗品/飲料の**両方**が同時に
  不足していたため、補充し合っても3+4=7件にしかならなかった
  （どちらかに余剰が無ければ、無いものは補充しようがない）。

「レビュー条件を満たす候補が本当に無かったのか」「検索ワードの多様性が
足りなかったのか」を切り分けるため、本番のAPIレスポンス・実行ログは
このセッションから直接確認できないが、上記の構造（便利グッズが単一
キーワード×5カテゴリーで代替手段が無い設計）だけでも、投稿済み履歴が
増えるほど不足が起きやすくなることを`tests/test_main.py`の既存の
シナリオテストで再現・確認できた。よって「検索ワードの多様性不足＋
投稿済み履歴の蓄積」を根本原因と判断した。

### 対応（追加探索・診断情報）

- **`config/settings.example.yaml`の`keywords`各エントリに、
  任意の`extra_keywords`（同じテーマ内の追加検索ワード）を追加できる
  ようにした。** 「暮らしの便利グッズ」5エントリに2〜3件ずつ、
  「消耗品・飲料」12エントリに1件ずつ追加した（掃除→お掃除グッズ/
  バス掃除グッズ/トイレ掃除グッズ、収納→収納グッズ/整理整頓グッズ/
  クローゼット収納、等。ユーザー提示の例をそのまま固定実装せず、
  既存のカテゴリー・テーマの範囲内で選定した）。
- **`src/main.py`に`_fetch_and_filter_keyword()`・
  `_collect_items_for_entry()`（新規）を追加。** 1つのkeywordsエントリに
  つき、本来の検索ワードの結果が既存の全フィルタ・重複除外を通した
  あとに0件だった場合だけ、extra_keywordsを前から順に試す（1件以上
  見つかるか、全て試し終わるまで）。**どの検索ワードでも既存の
  フィルタ処理（レビュー評価・件数・NGワード・テーマ判定・アルコール
  除外・耐久品除外・投稿済み履歴除外・同一実行内重複除外）をそのまま
  通すため、品質条件は一切緩めていない。** 本来の検索ワードで十分な
  結果がある通常の日は、追加のAPI呼び出しは発生しない（無駄なリクエスト
  増加を避けるため、0件のときだけ発動する設計）。
- **`storage.build_supply_diagnostics_markdown()`（新規）を追加し、
  GitHub ActionsのSummaryに「候補生成の内訳」セクションを常に表示する
  ようにした。** 便利グッズ・消耗品/飲料それぞれの実際の件数／目標件数、
  カテゴリーごとの条件通過件数（投稿済み除外後）・実際に試した検索ワード
  （extra_keywordsを使った場合は「本来のワード → 追加ワード」の形で
  表示）を一覧にする。目標件数に届かなかった日は、この表から「どの
  カテゴリーが／どの検索ワードまで試して／何件しか見つからなかったか」
  を確認できる。
- **10件に無理に合わせる変更はしていない。** `ranking.select_balanced_top()`
  自体（クロスグループ補充のロジック）や品質条件（レビュー4.0以上・
  100件以上等）は変更していないため、extra_keywordsを含めても本当に
  候補が見つからない日は、そのまま件数が少ないまま終了する
  （`tests/test_main.py`の
  `test_shortfall_is_not_padded_when_supply_is_genuinely_insufficient`
  で確認）。

### テスト

`tests/test_main.py`に次を追加した。
- extra_keywordsが0件時のみ使われること、十分な結果がある場合は
  一切検索されない（無駄なAPI呼び出しをしない）こと。
- extra_keywordsで見つかった商品にも既存の品質条件がそのまま適用される
  こと（条件を緩めていないことの確認）。
- 供給が本当に不足している場合、無理に10件へ埋めないこと。
- Summaryに候補生成の内訳（実際の件数／目標件数・試した検索ワード）が
  出力されること。

既存の`test_posted_history_exclusion_triggers_fallback_to_reach_ten`・
`test_convenience_shortfall_is_filled_from_consumable`は、
extra_keywordsが追加されたことで「本来のキーワードだけでなく
extra_keywordsも含めて完全に候補切れ」という条件に更新する必要が
あったため、`settings.example.yaml`から実際のextra_keywordsを動的に
取得するヘルパー（`_all_keywords_for_categories`）を使う形に書き換えた
（ハードコードした検索ワード文字列に依存していたテストの前提が、
今回の変更で変わったため。テストの意図＝クロスグループ補充の確認
自体は変更していない）。

## 30. 紹介文生成のカテゴリー誤フォールバック・数量抽出の改善（description-match-002）

2026-09-21生成分の実データで、以下4件の紹介文の問題が見つかった。

1. **ダスキンスポンジ**（`yamatoya-d:10000208`）：`GENERIC_TEMPLATES`
   ["キッチン消耗品"]の3つ目の悩み文（前回`description-match-001`対応時に
   追加したもの）が「使い捨てできる消耗品があると助かりますよね」と
   なっており、繰り返し使うスポンジのような商品にも「使い捨て」という
   確認できない性質を断定していた。
2. **水垢落とし研磨シート**（`suzumura-shoten:10000005`）：
   `PRODUCT_TYPE_TEMPLATES`のどのキーワードにも一致せず、カテゴリー
   「日用品」の`GENERIC_TEMPLATES`にフォールバックしていたが、その
   フォールバック文言が「ティッシュやトイレットペーパー」を名指しで
   前提にしており、無関係な商品（水垢のお手入れ用品）にまで
   そのまま使われ、商品と完全に無関係な文章になっていた
   （25章で修正した「洗剤」カテゴリーの誤フォールバックと同種の問題が
   「日用品」カテゴリーにも残っていたことになる）。
3. **2合炊飯器**（`calico-a1:10000058`）：`PRODUCT_TYPE_TEMPLATES`に
   「炊飯器」が登録されておらず、カテゴリー「時短」の`GENERIC_TEMPLATES`
   （一般的な時短家電の文言）にフォールバックしていた。
4. **国産はとむぎ茶**（`honjien-3:10003174`）：`_extract_quantity_phrase()`
   の正規表現パターンの優先順位により、「6g×50包（300g）」という
   セット内容表記から、単純に最初に見つかった数字「6g」だけを抜き出して
   いた（「g×個数」の組み合わせパターンが、単体の「g」パターンより
   優先されていなかったため）。

### 対応

- `GENERIC_TEMPLATES["キッチン消耗品"]`の該当する悩み文を「手間を減らせる
  消耗品があると助かりますよね」に修正（使い捨て可否を断定しない）。
- `GENERIC_TEMPLATES["日用品"]`のhook_text・worry_lines・worry_variants[0]
  を、特定の商品（ティッシュ・トイレットペーパー）を名指ししない、
  「日用品」という大分類だけに沿った安全な言い回しに修正。
- `PRODUCT_TYPE_TEMPLATES`に3件追加（スポンジ・水垢・炊飯器）。いずれも
  商品名から確認できる事実のみを使用（例：水垢は「トイレ・洗面台・陶器
  のお手入れ」、炊飯器は「普段のご飯炊き」等、大分類より具体的だが
  過度に断定しない範囲の言い回しにしている）。
- `FEATURE_CLAUSES`に5件追加（抗菌・ハードタイプ・洗剤不要・一人暮らし・
  ほったらかし）。商品名に明記されている場合だけ✔️メリットに反映され、
  推測では追加しない。
- `_QUANTITY_PATTERNS`の「重さ/容量×個数」パターンに`g`・`kg`も対象に
  含め（従来は`ml`/`L`/`ℓ`のみ）、単位に「包」「パック」も追加。
  これにより「6g×50包」のような表記を、単純な数字1つ（「6g」）ではなく
  意味のある単位のまとまりとして抜き出せるようにした
  （複雑化による誤抽出増加を避けるため、対応する単位・パターン数は
  必要最小限にとどめている）。

### テスト

`tests/test_description_generator.py`に`Sept21BatchRegressionTest`を
追加し、上記4商品の実際の商品名を使って、それぞれの問題が再発しない
こと（使い捨て断定なし・ティッシュ/トイレットペーパー混入なし・
水垢関連の固有情報反映・炊飯器の商品情報反映・6g×50包が正しく抽出
されること）を確認した。前回（25章・28章）修正した洗濯洗剤の食器洗い
誤用途・購入制限商品のまとめ買い矛盾の回帰確認も含めている。

既存の全テスト（商品検索条件・重複判定・GitHub Actionsの既存ワークフロー・
TikTok関連・前回までの紹介文修正等、今回変更していない機能のテスト）が
引き続き成功することを確認した。
