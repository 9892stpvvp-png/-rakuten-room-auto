# AI TASK

Status: READY
Owner: ChatGPT -> Claude Code
Task ID: reliability-004

## 目的
楽天ROOM/TikTok候補生成の既存機能を壊さず、日次生成で使うその他の永続JSON/テキスト書き込み経路を監査し、書き込み途中の中断による破損リスクを減らす。

## 今回の重点タスク
1. `CLAUDE.md` とこの `docs/AI_TASK.md` を読む。
2. `main` の最新状態を基準に、`data/`・`tiktok/` 配下など日次運用で更新される永続ファイルの書き込み経路を監査する。
3. PR #9および reliability-003 で対応済みの箇所は重複変更しない。
4. 直接上書きで破損リスクが残るJSON/テキスト保存があれば、既存の `src/atomic_io.py` を再利用して最小限の変更でアトミック化する。
5. 正常保存、例外時に既存ファイルを壊さないこと、既存挙動に回帰がないことをテストする。
6. 既存テストを実行し、変更に起因する失敗がないことを確認する。
7. `docs/AI_STATUS.md` に監査結果、変更、テスト結果、残課題を記録する。
8. 専用ブランチへcommit/pushし、PRを作成する。

## 安全ルール
- Application ID、Access Key、APIキー、トークン、パスワード、MFA情報などの秘密情報は表示・記録・コミットしない。
- `.env` や実値の資格情報はコミットしない。
- TikTok/楽天ROOMへの自動投稿、自動ログイン、自動購入は実装しない。
- 課金が発生する外部サービス、動画生成API、アカウント設定変更は追加しない。
- 商品検索条件、重複判定、紹介文生成の意味を不要に変更しない。
- 変更不要と判断した場合は無理にコード変更せず、監査結果を `docs/AI_STATUS.md` に記録してPRを作成する。
